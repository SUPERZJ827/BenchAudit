# BenchCore — Claude 工作规范

## 项目概览

BenchCore 是一个 benchmark 质量缺陷检测 pipeline。核心代码在 `benchcore/`，实验结果在 `reports/`，数据集在 `/home/zhoujun/llmdata/datasets/`。

---

## 编码规范

### 1. 先读再写

修改文件前必须先读它，而不是只扫一眼。查看类似功能是怎么实现的（例如新增 auditor 前先看现有 auditor 的结构）。复用已有的工具函数，不要重新发明。检查文件顶部的 import，跟随项目已有的库选择。

### 2. 想清楚再动手

没搞清楚任务前不写代码。先说明假设和取舍。多种方案时给出推荐，最多列两三种。需求不清楚时停下来问，不要用"看起来合理"的代码补理解空白。

### 3. 保持简单

写能解决当前问题的最少代码。避免：
- 过早抽象（只有一个实现的接口/基类）
- 臆想式错误处理（处理根本不会发生的情况）
- 不必要的可配置（把永远不会变的值做成参数）

### 4. 外科手术式修改

diff 越小越好。不要碰用户没有要求修改的地方。匹配现有代码风格（引号、命名、格式）。不要顺手重构无关代码，不要重新格式化整个文件。每行改动都应能说出和任务直接相关的理由。

### 5. 验证

修 bug 时先写能复现 bug 的测试，看到失败，再修，再看到通过。改动前后都运行现有测试。测试关注行为，不关注实现细节。

运行测试：
```bash
python -m unittest discover tests/ -v
```

### 6. 目标驱动执行

多步骤任务执行前先列计划，让用户确认方向再开始。模糊需求转化为可验证的具体目标。

### 7. 调试

出问题时调查，不要猜。完整读错误信息和 stack trace。先复现再修。一次只改一处。卡住时说出已经试过什么、现象是什么、哪里不确定。

### 8. 依赖

不轻易添加新依赖。优先用项目已有的库（标准库 > 已有第三方 > 新引入）。必须添加时说明理由。

### 9. 沟通

说明做了什么、为什么这么做。主动指出隐患和不确定性。不解释用户已经知道的东西。精确表达不确定性（"我不确定 X 是否支持 Y"比"应该能工作"更有用）。

### 10. 常见失败模式（要避免）

- **大杂烩**：加一个功能顺手重构半个代码库
- **错误抽象**：为只出现一次的问题构建通用方案
- **隐形决策**：做了架构选择但没告诉用户
- **只有 happy path**：忽略 API 500、文件不存在、空输入等
- **知识幻觉**：使用不存在的 API 或幻想出来的库特性
- **风格漂移**：用自己偏好的风格而不是项目风格
- **失控重构**：修一个问题连锁扩散，忘了最初任务

---

## 用户补充的工程原则（长期适用）

> 权衡：本节优先保障严谨性，而非速度。简单任务可酌情灵活处理。

### 编码前先思考

- 不做无端假设；发现不确定性时主动说明，而不是用猜测填空。
- 动手前明确假设、证据、取舍和成功标准。
- 存在多种解读时列出差异，不要隐式选择会改变任务范围的解释。
- 发现更简单的实现时主动说明；若当前方向可能不合理，应直接提出异议。
- 需求仍不明确且会影响实现方向时先暂停并询问，不在事后用解释补救。

### 优先保持简洁

- 用满足当前需求的最少代码解决问题，不实现需求之外的功能。
- 仅使用一次的逻辑不作过度抽象；不添加未被要求的可配置项、扩展点或防御性分支。
- 不为不可能出现的场景增加复杂异常处理。
- 如果一个明显更短、更直接的实现可以达到相同效果，优先采用它。
- 自检：资深工程师看到这个设计会不会觉得过于复杂？若会，就简化。

### 精准改动

- 只修改与当前需求直接相关的内容；只清理由本次改动产生的废弃代码。
- 编辑已有文件前先完整阅读相关上下文；不顺手重构相邻代码、注释或格式。
- 没有问题的代码保持原样；不删除原本就存在的死代码，除非用户明确要求。
- 对每一行改动都能说明它对应的需求或验证目标。

### 目标驱动执行

- 把任务转换成可验证的目标，并持续验证直到达到目标或明确报告阻塞。
- 新增校验：先写非法输入测试，再实现逻辑并让测试通过。
- 修复 bug：先写可复现测试，确认失败后再修复，最后确认通过。
- 重构：验证重构前后的行为一致，而不是只确认代码能运行。
- 多步骤任务开始前给出简短计划；每一步包含执行动作和验证方式。
- 成功标准含糊且无法安全假设时，先向用户确认。

### 生效检查

每次改动前后检查：是否作了未经说明的假设？是否改了需求之外的内容？是否有对应的验证？若任一答案不清楚，先停下来说明问题。

本节生效的标志是可观察的结果，而不是复述条款：代码差异里不必要的改动变少，因过度复杂导致的返工变少，澄清类问题出现在编码之前而不是出错之后。
