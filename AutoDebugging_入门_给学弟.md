# Benchmark Auto-Debugging 入门

> 面向:研究方向新同学。假设你会 Python、了解 LLM 的基本用法,但没做过"给 benchmark 查错"。
> 读完目标:理解这个方向在做什么、几个核心概念、并能独立跑通第一个练习任务。

---

## 1. 一句话:我们在做什么

现在评价一个 AI 模型/agent 好不好,靠的是各种 **benchmark(测试集)**。但 benchmark 是人做的,**会有错**:题目有歧义、标准答案本身写错了、评分标准过于苛刻、甚至答案直接泄漏在题目里。benchmark 一旦有缺陷,"谁强谁弱"的排名就不可信。

我们做的事:**用自动化方法(LLM + 程序)去检测 benchmark 自身的缺陷**,让评测更可信。这叫 benchmark auto-debugging。注意——我们不是在评价模型,而是在**给评价模型的那把尺子挑毛病**。

---

## 2. 先建立几个直觉(真实例子)

- **答案泄漏(SWE-bench)**:一道题让 AI 修一个 bug,结果题目描述里有人直接写了"把这行改成 `X` 就好了",而 `X` 正是标准答案。AI 照抄即可"通过"——这题测不出真本事。
- **值错(Workspace-Bench)**:评分标准写"报告里应有 6 个已批准订单",但原始数据里其实是 7 个。标准答案本身错了。
- **过约束**:任务只说"写一份分析报告",评分标准却硬性要求"必须建议建立安全库存预警系统"。一个正确但写法不同的报告会被误判为错。

这三类就是我们要自动找出来的"缺陷"。

---

## 3. 核心概念(术语速查)

| 术语 | 含义 |
|---|---|
| benchmark / 数据集 | 一堆任务 + 每个任务的评判标准 |
| task | 要 AI 完成的事(答题 / 改代码 / 写报告) |
| gold answer / gold patch | 标准答案 / 标准修复代码 |
| rubric | 评分标准(自然语言的一条条判据);Workspace-Bench 用它打分 |
| test(单元测试) | SWE-bench 用测试当评判标准(客观、可执行) |
| defect(缺陷) | benchmark 自身的错(见第 4 节) |
| candidate / confirmed | 候选 / 确认 —— **本方向最重要的区分**(见第 5 节) |

---

## 4. 缺陷分类(目前关注的几类)

| 代号 | 类型 | 例子 |
|---|---|---|
| B1 | 数据缺失 | rubric 要"按医院等级统计",但输入数据没有"等级"这一列 |
| B2 | 值错 | rubric 说 6,重算实际是 7 |
| B4 | 过约束 | 开放任务被钉死一个特定答案/结论 |
| B5 | 结构错 | 硬性要求特定文件名/sheet 名,任务并没要求 |
| 泄漏 | 答案泄漏 | 题目描述里直接给出了标准答案 |

---

## 5. 最重要的一课:候选 vs 确认(为什么这方向难)

你当然可以让 LLM 判断"这条 rubric 是不是过约束"。但我们实测发现:**LLM 的这种主观判断不可复现**——同一条重跑 10 次,难的 case 结果翻来覆去,就像抛硬币;而且换更强的模型(我们试过 GPT-5.5)也不解决。

所以我们把结果分两档:

- **确认(confirmed)**:能对照**客观标准**验证的,才自动确认。例如:
  - "输入数据里确实没有等级列"(客观事实,可查)
  - "重算确实得 7 不是 6"(可复现的计算)
  - "这行答案确实逐字出现在题目里"(字符串匹配)
- **候选(candidate)**:靠 LLM 主观判断的(过约束、结构好坏),只能作为"可疑项"交给人看,**不能自动下结论**。

> **核心原则:只有能客观 grounding(对照数据/代码/测试核验)的缺陷,才能自动确认;主观判断只能当候选。**
> 这句话是整个方向的地基。你会反复用到它。

---

## 6. 方法框架(简化版)

**两级流水线**:先用程序做**字面过滤**(便宜、召回高),再用 **LLM 做语义确认**(判断字面命中是不是真缺陷)。

以"SWE-bench 泄漏检测"为例:
1. **第一级(程序)**:查"gold patch 新增的代码行,有没有逐字出现在题目描述里"——纯字符串匹配,秒级。
2. **第二级(LLM)**:判断"这段代码是作为**修复方案**给出的(真泄漏),还是只是 **bug 复现代码**(假阳性)"。

为什么非要两级?因为第一级单用**精度只有 52%**(一半是假阳性,比如题目里贴的错误复现代码恰好和答案某行相同),加上第二级语义确认后才可靠。这个"字面召回 + 语义确认"的套路,在本方向里到处都是。

---

## 7. 数据集地图

| 数据集 | 任务形式 | 客观性 | 适合练什么 |
|---|---|---|---|
| **SWE-bench** | 修 GitHub bug,单元测试判分 | 高(有测试) | **泄漏检测(最容易上手)** |
| SVAMP / MMLU | 数学题 / 选择题,唯一答案 | 高 | 标准答案错误检测 |
| Workspace-Bench-Lite | agent 做办公任务,rubric 打分 | 低(主观 rubric) | 理解"主观为什么难自动确认" |

一个规律:**数据集越客观可验证,自动确认越容易出成果**;越主观(靠 rubric/人评),越只能出候选。

---

## 8. 你的第一个练习(以练代学)

**任务:在 SWE-bench_Lite 上复现"problem_statement 答案泄漏"检测。** 最简单、最客观、当天能出结果。

```python
from datasets import load_dataset
ds = load_dataset("princeton-nlp/SWE-bench_Lite", split="test")  # 300 条

def added_lines(patch):
    # gold patch 里真正新增的实质代码行(+ 开头,排除 +++,够长、非注释)
    out = []
    for ln in (patch or "").splitlines():
        if ln.startswith("+") and not ln.startswith("+++"):
            c = ln[1:].strip()
            if len(c) >= 12 and not c.startswith(("#", "//", "*")):
                out.append(c)
    return out

for e in ds:
    leaked = [a for a in added_lines(e["patch"]) if a in (e["problem_statement"] or "")]
    if leaked:
        print(e["instance_id"], f"{len(leaked)} 行泄漏", leaked[:1])
```

做完这些:
1. 跑出候选清单(会有 ~20 条)。
2. **人工读几条原文**,判断是真泄漏还是假阳性(题目里的复现代码)。你会发现:泄漏行占比越高的越可能是真的。
3. (进阶)接上 LLM,对每条判断"是修复方案还是复现代码",过滤假阳性,得到 ~11 条真泄漏。

跑通后,你就亲手体会了本方向最核心的"字面召回 + 语义确认"两级范式。参考实现问学长要(scratchpad 里有原型)。

---

## 9. 接下来几周的路线(循序渐进)

1. 泄漏检测(字面)→ 接 LLM 语义确认。
2. 扩到更大的 SWE-bench(Verified 500 / 全集 2294),给可靠的泄漏率。
3. 换一类缺陷练手:B1 数据缺失(在有原始输入文件的数据集上)。
4. 读现有 auditor 代码,理解 Plan → Exec → Eval 的完整流程。

---

## 10. 新手常见误区(提前避坑)

- **别信小样本**:一个想法在 5–10 个精选案例上"看着 work",在全量上经常翻车。**一定要在完整测试集上量化**(我们为此踩过好几次)。
- **别把 LLM 主观判断当确认**:它不可复现。只有客观可核验的才自动确认。
- **分清泄漏来源**:SWE-bench 的 `hints_text`(issue 评论)默认**不喂给 agent**,里面有答案是"已知且已规避"的,别把它当成影响评测的新缺陷;真正要紧的是 `problem_statement` 泄漏。

---

## 术语 & 资源

- 代码库:`/home/zhoujun/llmdata/after623`(auditor 在 `benchcore/` 和 `scripts/`)
- 跑测试:`python -m unittest discover tests/ -v`
- 有问题先问学长,或看《AutoDebugging_方法与现状_导师版.md》里的实验结论与教训。
