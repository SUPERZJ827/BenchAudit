# Agent 检测方法库 v1：每类缺陷用什么方法查

> 配套 `benchmark缺陷分类法_v2.md`。把每个缺陷格子映射到**具体检测方法**，并按"**要不要理解语义**"分成三个梯度。
> 核心原则：**能用代码确定性查的，绝不交给 LLM**；必须语义的，套上今天实验得出的硬约束（task_type 门控 + 多跑众数 + 证据门控）。

---

## 1. 三个检测梯度（按对语义的依赖）

| 梯度 | 方法本质 | 要理解语义吗 | 可靠性 | 成本 | 单次可信吗 |
|---|---|---|---|---|---|
| **T1 确定性/规则** | 代码解析、查字段、重算、正则、结构比对 | **不需要** | **高** | 极低 | **可** |
| **T2 规则定位 + 轻语义** | 抽取(数字/字段名/实体) + 模糊/向量匹配 | 少量 | 中高 | 低 | 基本可，需轻复核 |
| **T3 纯语义** | LLM 理解任务意图后判断 | **必须** | 低(今天证明不可复现) | 高 | **不可**，必须聚合 |

> **T3 的硬约束（来自今天的归因实验，不是可选）**：
> ① 先判 `task_type`，确定性任务直接跳过 B3/B4；② 同一判断多跑取**众数**；③ 必须产出**证据**(引用任务原文那句 / 输入里查过的那列)才算数。

---

## 2. 缺陷 → 方法 映射（重点表）

| 缺陷 | 主检测方法 | 梯度 | 用到的工具 | 已有? |
|---|---|---|---|---|
| **A2** 输入损坏/不可解析 | 解析所有输入文件，捕获失败/空表/乱表头 | **T1** | `read_file` 解析+异常 | ✅ file_reader.py |
| **A3** 评分器期望值算错 | 从输入按 rubric 口径**重算**，与 rubric 写的值比对 | **T1** | `recompute`(代码执行) | ✅ output_l1_verifier.py |
| **B1** 评分器要的数据输入里没有 | 从 rubric 抽"要查的字段/口径" → 在输入列名/内容里**找** | **T2** | `extract_fields` + `match_terms` + `find_in_inputs` | ⚠️ 现为 LLM(rubric_data_verifier)，可下沉 |
| **B2** 评分器期望值与输入对不上(含单位) | 同 A3，重算+单位归一后比对 | **T1** | `recompute` + 单位归一 | ✅ output_l1_verifier.py |
| **B5** 脆弱约束(精确文件名/表名/阈值) | 正则抽 rubric 里的文件名/sheet名/阈值 → 检查任务原文**是否要求过** | **T2** | `extract_literals` + `grep_task` | ⚠️ 待建(规则即可) |
| **B7** 输出契约与任务冲突 | 抽 T 要求的格式 vs G 要求的格式，做集合比对 | **T2** | `extract_format` ×2 + 比对 | ⚠️ 待建 |
| **A1** 任务歧义/欠定义 | LLM 读 T，判断是否有多解/缺约束 | **T3** | `semantic_judge` | ✅ 语义审计器 |
| **A4** 评分器模糊/不可验证 | LLM 判 rubric 是否可客观判分 + 多判一致性 | **T3** | `semantic_judge` ×N | ✅ 语义审计器 |
| **B3** 过度约束 | 先判 task_type；主观任务下 LLM 判 G 是否钉死唯一答案 | **T3** | `classify_task_type` + `semantic_judge` ×N | ✅ scan_hf_tasks.py |
| **B4** 答案泄漏 | 先判 task_type；判 G 是否写出"该 agent 推导的结论" | **T3** | 同上 | ✅ scan_hf_tasks.py |
| **A5/B6** 参考输出/覆盖(WB 不适用) | 对 O 跑 G / 构造反例 | T1+T3 | — | WB 无 O |

---

## 3. Agent 工具箱（要提供的基础方法）

按"先确定性、后语义"排：

**确定性层（T1/T2 的积木）**
1. `read_file(path) -> text+tables` —— 通用解析 xlsx/docx/pptx/pdf ✅已有
2. `get_schema(file) -> [列名/sheet名]` —— 抽结构 ⚠️小封装
3. `find_in_inputs(term) -> 命中位置` —— 跨输入找某字段/值 ✅search_file 雏形
4. `recompute(口径, inputs) -> 值` —— 代码沙箱重算 ✅output_l1
5. `extract_literals(rubric) -> {数字, 阈值, 文件名, sheet名}` —— 正则/NER ⚠️待建
6. `match_terms(a, b) -> 相似度` —— 模糊/向量匹配字段名 ⚠️待建
7. `grep_task(literal) -> 任务原文是否提及` —— 把"脆弱约束/泄漏"落到可验证 ⚠️待建

**语义层（T3，受约束使用）**
8. `classify_task_type(task) -> deterministic|subjective` —— **总开关**，要缓存+多判 ✅
9. `semantic_judge(T, G, defect_type) -> {flag, 证据}` —— 多跑取众数、强制给证据 ✅需加聚合
10. `cite_evidence(flag) -> 引用 T 那句 / I 那列` —— 证据门控 ⚠️待建

---

## 4. 关键设计原则：**能下沉就下沉**（把"看似语义"拆成"语义提线索 + 确定性验证"）

很多缺陷**怀疑靠语义、确认靠规则**，应尽量把"确认"这步从 T3 拉到 T1/T2：

- **B1（数据缺失）**：现在整条交给 LLM(不稳)。下沉版：① 正则/小 LLM 从 rubric **抽出**"要按 X 统计" → ② 用 `match_terms` 在真实列名里找 X → ③ **找不到才报**。判定从"LLM 拍脑袋"变成"列名匹配失败"，可复现。
- **B4（答案泄漏）**：① LLM 只负责**抽出** rubric 里的具体结论/数字（线索）→ ② `grep_task` 确认任务是否要求 agent"自己分析/发现"该结论 → ③ 两者都成立才报。判定有了确定性锚点。
- **B5（脆弱约束）**：纯规则——`extract_literals` 抽 sheet 名/阈值，`grep_task` 查任务没提 → 报。几乎不用语义。

> 这就是"验证-门控审计器"的具体落法：**LLM 出线索、代码下结论。**

---

## 5. 哪些**逃不掉语义**（必须 LLM，且只能靠聚合压住）

- **A1 任务歧义**、**A4 rubric 模糊**、**B3 过度约束**的**判定**部分——因为要判断"任务到底想要什么、这算不算过严"，是意图理解，规则替代不了。
- 对这几类的诚实态度：**不追求单次准确，追求"多跑一致 + 多信号"**。今天实验已证明单次不可复现，所以它们天然只能产"候选"，靠 T1/T2 的确定性证据或多次一致来兜底。

---

## 6. 一句话总结

**把检测能力做成一个"梯度漏斗"**：先用 T1/T2 的确定性方法把能查的查掉（可信、便宜、可复现）；剩下真要懂意图的丢给 T3，但只当"线索生成器"，再用 T1/T2 验证 + 多跑取众数把它钉死。**agent 的智能不在于"用大模型判得准"，而在于"知道每个格子该用哪个梯度的方法"。**
