# Platinum 盲测运行协议 V3 —— 闸门口径修订

> 日期：2026-08-03
> 状态：**frozen before any paid API request**
> 父协议：`PLATINUM_BLIND_HOLDOUT_RUN_PROTOCOL_V2_20260803.md`（不修改、不作废）
> 授权：用户批准 ¥50 预算、¥45 硬停

## 0. 本次只改一件事

V2 的 dry-count 逐项结果：

| 闸门 | 结果 |
|---|---|
| `materialized_897_unique` | ✅ |
| `method_set_exact` | ✅ |
| `field_isolation` | ✅ |
| `no_new_adapter_branch` | ✅ |
| `three_run_calls_at_most_21528` | ✅ |
| `estimated_cost_at_most_cny_60` | ✅ |
| **`planning_token_proxy_at_most_50000000`** | ❌ 79,782,468 |

**唯一失败项是 token 规划代理。** 而它所代理的量——成本——自己的闸门已经通过。

## 1. 修订内容

1. **移除** `planning_token_proxy_at_most_50000000` 作为 go/no-go 判据；token 数继续测量、继续记录、继续报告，**只是不再作为闸门**；
2. **收紧**成本闸门：`¥60 → ¥45`（硬停），预期 ¥37.67，上界代理 ¥45.39；
3. 其余**一字不改**：897 条样本、18 项 method 集（`f931de8e…`）、flag 定义、指标口径、三跑、独立空缓存、Git 祖先链。

## 2. 为什么这不是移动球门

| 条件 | 满足情况 |
|---|---|
| 只动**纯资源参数** | ✅ 仅 token 上限与成本上限；样本量/method/flag/指标均未触碰 |
| 在**任何科学结果产生之前** | ✅ 尚未发出任何付费请求，truth 未解封 |
| 新值**独立论证**而非照估算值配门 | ✅ ¥45 由用户批准的 ¥50 预算反推，不是由 79.8M 反推 |
| 方向 | ✅ **收紧**真实约束（¥60→¥45），移除的是一个已知过估的代理 |

**精确损失**：token 上限不再独立于成本估计。若将来 provider 单价大幅变动，成本闸门的保护力会随之变化；token 数仍记录，可事后核查。

依据：token 代理按官价（$0.14/1M 输入、$0.28/1M 输出）折算约 ¥90，而经验估计为 ¥37.67，**代理过估约 2.4 倍**。闸门开在一个已偏离其代理对象的量上。

## 3. 硬停与停止条件

| 项 | 值 |
|---|---|
| 成本硬停 | **¥45** |
| 三跑调用上限 | 21,528（沿用 V2） |

任一情况立即停止并如实报告已花费与已完成轮次：

1. 累计成本估计超过 ¥45；
2. 任一运行 operational failure > 5%；
3. 出现任何 LLM 派生的 `confirmed`；
4. 任一运行的 `methods_run` 与冻结 18 项不完全一致（`NOT_COMPARABLE_METHOD_SET`）；
5. 传输路径不是 `proxy` 或 `direct`（须记录，不得未知）。

## 4. 预注册：更强模型探针的触发条件

**本次不执行。** 触发器现在写定，避免看到结果后再决定：

> 若 Layer B 的 revised recall 三跑中位数 **< 0.50**，则可追加一次 `deepseek-v4-pro` 单跑探针（同 897 条、同 method 集、同口径，约 ¥41），用于分离"方法不行"与"模型不行"。
>
> 触发后**必须另行取得预算批准**；无论探针结果如何，**两个模型的数都报**，不得只报较好的那个。

## 5. 执行顺序（Git 祖先链，沿用 V2）

```
1. 本协议提交（尚未运行）
2. 三次运行 → 三份预测产物 → 提交
3. prediction-lock 提交（预测集 SHA-256）
4. 才解封 sealed truth（8e09ebc3…）并评分 → 提交
```

评分入口固定为 V2 指定的唯一脚本；scoring 代码须为三次 prediction 提交的共同祖先。

## 6. 报告口径（沿用 V3 selection protocol，此处重申）

- 头条 = 三跑**中位数**；三值、min–max、SD 全列；
- **Layer B 为有功效的主结果**（170 阳性）；**Layer A 仅存在性**（25 阳性，其中 22 为 `rejected`，不得作 gold-error 结论）；Layer C 探索性；
- 主指标为 **recall 与 specificity/FPR**（与阳性率无关）；
- **precision 仅作为阳性率 π 的函数报告**，并给出 π=6.491% 下的换算；
- 禁止未加权 pooled F1。
