# Benchmark 缺陷分类法 v2（通用骨架 + agentic 扩展）

> 目标：给"自动探错 agent"一份**通用的、按部位组织的**缺陷菜单。
> 设计原则：**通用性来自结构、贴地性来自例子**。每个缺陷类型都标注：定义 / 所在部位 / 真实 Workspace-Bench 例子(带任务号) / 检测信号 / 可靠性 / 与旧 BenchAudit 分类的对应。
> 向下兼容：旧 BenchAudit 的 6 类（Gold / Specification / Option / Table / Unit / Answer-Contract）是本体系在"离散问答 benchmark"上的特例（见每条末尾 `↩旧:`）。

---

## 0. 通用骨架：任意 benchmark item 的 4 个部位

任何 benchmark 的一道题，都可拆成这几块（不一定全有）：

| 代号 | 部位 | 在不同 benchmark 里的形态 |
|---|---|---|
| **T** | 任务 / 指令 | 题干、instruction、issue 描述 |
| **I** | 输入 / 上下文 | 输入文件、初始环境状态、给定数据 |
| **G** | 评分器 | **rubric / 单元测试 / reward 函数 / gold answer** —— 都是"评分器"的不同形态 |
| **O** | 参考输出 | 期望产物、reference solution（可选；很多 benchmark 没有，由 agent 现产） |

> **Workspace-Bench 的形态**：T=任务描述，I=xlsx/docx/pptx/pdf 输入，G=人写 rubric 条目，O=无（agent 现产、由 G 打分）。
> → 所以 WB 上**O 相关的格子用不上**，这正是"discovery 阶段按部位裁剪"的意义。

**两类缺陷**：
- **A 类 = 单部位自身缺陷**（intrinsic）：某个部位本身就坏/不清楚。
- **B 类 = 跨部位不一致缺陷**（consistency）：两个部位对不上 —— **agentic benchmark 的缺陷绝大多数在这里。**

---

## A. 单部位自身缺陷（intrinsic）

| 编号 | 缺陷 | 部位 | 定义 | WB 真实例子 | 检测信号 | 可靠性 |
|---|---|---|---|---|---|---|
| **A1** | 任务歧义/欠定义 | T | 任务表述有多种合理解读，或缺关键约束 | 候选：多条任务只说"生成报告"未定口径 | LLM 读 T | 中 |
| **A2** | 输入损坏/缺失/不可解析 | I | 文件读不出、乱表头、合并单元格、字段缺 | 普遍：xlsx 乱表头导致字段抽取失败 | **代码解析**(确定性) | **高** |
| **A3** | 评分器期望值算错 | G | rubric 写的期望数值/gold 本身就是错的 | （WB 待查；离散版即 gold 标错） | **代码从 I 重算**(确定性) | **高** ↩旧:Gold error |
| **A4** | 评分器模糊/不可验证 | G | 标准主观("合理"、"无编造")→ 打分不一致 | 候选：多条 rubric 用"分析全面"等模糊词 | LLM 判 + 多判一致性 | 低 |
| **A5** | 参考输出错误 | O | 给定的 reference solution 本身不对 | WB 无 O，**不适用**（保留给别的 benchmark） | 对 O 跑 G | 中 |

> 注：旧"**Option error**"(选项本身有问题) = A 类在 MCQ 上的特例（评分器=选项+gold 的特殊形态）。

---

## B. 跨部位不一致缺陷（consistency）——**重点**

| 编号 | 缺陷 | 涉及部位 | 定义 | WB 真实例子(已确认) | 检测信号 | 可靠性 |
|---|---|---|---|---|---|---|
| **B1** | 评分器要的数据输入里没有 | G↔I | rubric 检查的字段/数据在输入中根本不存在 → 正确 agent 也被扣 | ✅ **仓敏_3**：rubric 要按医院等级统计，3 个输入文件无等级字段 | **代码查字段存在性**(确定性) | **高** ↩旧:部分Table/Unit |
| **B2** | 评分器期望值与输入对不上 | G↔I | rubric 期望值能从输入重算，但对不上（含单位错） | （WB 待查） | **代码重算比对**(确定性) | **高** ↩旧:Unit error |
| **B3** | 过度约束 | G↔T | 开放/主观任务被评分器钉死成唯一答案/分数/结构 | ✅ **李汶松_52**(强制评分体系+总分24)、✅ **id=35**(临时权限"不超过3天"等作者方案) | LLM(需先判 task_type) + 多判众数 | 中(强模型稳) |
| **B4** | 答案泄漏 | G↔T | 评分器直接写出"本应由 agent 推导的结论" | ✅ **id=244**(rubric 写死"评分标准严重失真"等该自己发现的结论) | LLM(需先判 task_type) + 多判众数 | 中 |
| **B5** | 脆弱约束 | G↔T | 评分器要求任务没规定的精确文件名/表名/阈值 | 候选：**仓敏_3** 强制 3 个精确工作表名 + 一个任务没要求的"数据说明"sheet | LLM + 比对 T 原文 | 中 ↩旧:Answer-Contract |
| **B6** | 评分覆盖不足/可被钻 | G↔O | 测试/reward 覆盖不到真正正确解，或空解也能过(reward hacking) | WB 无 O/测试，**不适用**（保留给 SWE-bench/τ-bench 类） | 反例构造 | 中 |
| **B7** | 输出契约与任务冲突 | T↔O | 任务说的产物格式与评分要求的格式互相矛盾 | 候选：任务"最好包含图"，rubric 强制 .pptx | 比对 T↔G | 中 ↩旧:Answer-Contract |

---

## C. 这套分类法怎么喂给"探错 agent"

agent 不是把所有格子盲跑一遍，而是**先探后查**：

```
阶段0 · 结构发现(看几个样本)
  读 N 个 item → 判定：有哪些部位(T/I/G/O 哪些存在)？评分器是什么形态(rubric/测试/gold)？
  → 裁剪出"本 benchmark 适用的格子子集"
  例：WB → 有 T/I/G、无 O → 砍掉 A5/B6/B7，保留 A1-A4 + B1-B5

阶段1 · 逐部位检测(按格子选对的工具)
  确定性格子(A2/A3/B1/B2) → 调代码工具(解析/重算/查字段)   ← 可靠
  主观格子(A1/A4/B3/B4/B5) → 调 LLM，但必须:
      ① 先判 task_type(确定性任务直接跳过 B3/B4)   ← 今天实验证明这是"总开关"
      ② 同一判断多跑取众数(单次 LLM 不可复现)        ← 今天实验证明的

阶段2 · 交叉验证 + 证据门控(升"候选"为"确认")
  一条缺陷要确认，必须: ≥2 信号命中 / 独立模型复核 / 确定性锚定 三者之一
  主观类还要求 agent 给出证据(引用 T 原文那一句 / 输入里查过的那一列)
```

> **"灵活判断查哪些位置"= 阶段0**：分类法是通用菜单，agent 按真实结构点菜。
> 这样**换一个 benchmark**(比如 HealthBench)也能跑：discovery 发现它有 T/I/G、G 是加权 rubric → 自动启用同一批 B1-B5 格子。

---

## D. 与旧 BenchAudit 的关系（一句话）

旧 6 类是"离散问答"这一形态下的特例；v2 把"部位"从 3 块(题/选项/gold)推广到 4 块(T/I/G/O)，把"评分器"从 gold 推广到 rubric/测试/reward，于是同一套"部位×(自身缺陷/跨部位不一致)"框架同时覆盖 QA、代码、agentic 三类 benchmark。

| 旧 BenchAudit | v2 对应 |
|---|---|
| Gold error | A3 |
| Specification ambiguity | A1 |
| Option error | A 类(MCQ 评分器特例) |
| Table error | A2 |
| Unit error | B2 |
| Answer-Contract error | B5 / B7 |

---

## E. 诚实边界（汇报时要讲）

- 标 ✅ 的格子(A2/B1/B3/B4 + 候选 B5)是**WB 真实数据里出现过**的；标"待查/候选/不适用"的是**通用保留**或**尚未在 WB 证实**，不要当成已发现的缺陷。
- 可靠性列直接来自今天的实验：**确定性格子可信、主观格子需 task_type 门控 + 多判众数**。这条是 agent 设计的硬约束，不是可选项。
