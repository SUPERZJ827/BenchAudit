# 给学弟：第一轮 SVAMP 实验的反馈与下一阶段安排

> 日期：2026-07-30
> 对应材料：`EXPERIMENT_ENV.md`、`SVAMP_RESULTS.md`、`SVAMP_ERROR_ANALYSIS.md`

---

## 一、先说你做得好的地方（这些不是客套，是具体的）

**1. 你写了 `EXPERIMENT_ENV.md`，而且如实记录了脏工作树。**

```
M benchcore/llm_client.py
M configs/llm_deepseek.json
M scripts/run_direct_llm_baseline.py
M scripts/run_svamp_pilot.py
M tests/test_llm_client.py
```

很多人第一次做实验根本不会记这个，记了的也可能把"有未提交改动"这条藏起来。你写出来了 —— 正因为你写了，我们才能立刻定位到"这轮结果不可复现"这个真问题。**如实记录不利信息，是做实验最重要的习惯，你一上来就有了。**

**2. 你自己写了口径说明，而且写对了。**

> 以上 BenchAudit 数字是 candidate/review 候选检测能力，不是系统自动 confirmed 的错误数量。本次报告的 confirmed 层为 0。

review 和 confirmed 的区别是这个项目的核心纪律，你没人教就分清了，还主动标出来。

**3. 历史结果回退，你报了没藏。**

BenchAudit F1 从 0.914 掉到 0.727，这个数字对你不利，你放在主文档里，还列了六个可能原因且没有挑一个方便的下结论。这是对的做法。

**4. 你不是只数 FP，你逐条裁决了。**

`SVAMP_ERROR_ANALYSIS.md` §C 那一列"真正误报 / 题目存在争议但证据不足 / Platinum 标签可能漏标"—— 18 条 FP 里真正误报只有 10 条。这说明 **FP=18 是上界**。这个判断是你做出来的，很有价值。

**5. 你拒绝了两个方便的做法。**

> 也不会在本轮分析中修改原始标签

以及 §G 里：

> 不会像简单提高判定阈值那样，以牺牲 Recall 为代价减少误报

不改标签让数字好看、不用调阈值糊弄精度 —— 这两个判断都对。

**6. §F 的失败模式分类表是可复用资产。**

带 item ID 的分类表，后面几轮都能拿来对照。而且下面要讲的新方向，线索就藏在这张表里。

---

## 二、为什么我们要关掉"提高候选质量"这条线

**先说清楚：不是你的实验做错了，是这条线我们已经用三个独立实验证明走不通。** 你入场时不知道，这是我的责任。

### 三个负结果

| 实验 | 结果 |
|---|---|
| **Exact router**（零 API 精确规则） | 独立 holdout 上找到 3 条 TP，**全部已被基线找到**，增量 reviewed TP = 0，新增 2 条无标注候选 → FAIL |
| **A′**（结构化拒绝路由） | 候选率 52.1%→46.4%，逻辑调用 −9.96%，但 family recall **84.2%→63.2%**（−21 个百分点）→ 不是 Pareto 改进，FAIL |
| **A″**（约束残差规则） | 开发集 PASS（16/19），冻结的内部验证集 **4/7**（门槛 6/7）→ 规则没有迁移，FAIL |

### 三次失败的共同根因（这部分对你有用）

**(a) 模型自报的 confidence 没有信号。**

A′ 那轮，五个预注册阈值 `0.50/0.60/0.70/0.80/0.90` 给出**完全相同的 188 条候选**，因为被选中的候选里最低置信度就是 0.90。模型对什么都很自信。**所以"调阈值"这条路从物理上就不存在** —— 你在 §G 里主动排除了这个做法，方向是对的。

**(b) 结构化 JSON ≠ 结构化推理。**

A′ 漏掉的 7 条里有 **3 条**，模型自己写的 `brief_reason` 和它自己选的 `reason_code` 直接矛盾。比如一条 rubric 明确要求六个具名章节，模型的解释写着 "Requires **specific sections**"，选的类别却是 `general_quality`，证据来源标 `intrinsic`，引文留空。

**这个现象和你发现的"答案字段自相矛盾"是同一个东西。** 你在 162/599/687/934/974 上看到的，我们在 Workspace 上看到的，根因一样：让 LLM 同时理解证据、选类别、下判断、给置信度，它会给你一个格式完美但内部不自洽的 JSON。

**(c) 候选层的指标本身就不可靠。**

Workspace 那边 reviewed 参考只覆盖 28/405 ≈ 7% 的数据。你这边也一样 —— 你自己算出 18 条 FP 里只有 10 条是真误报。**当 93% 的数据没有标签时，你可以把 precision 推来推去，却不知道自己有没有真的改进。**

### 战略上的理由

看看现在最前沿的三个工作，终点都在哪：

| 工作 | 终点 |
|---|---|
| STING（SWE-bench Verified） | 生成定向测试，**人工/差分验证** |
| Fantastic Bugs（九个 benchmark，Precision@50 84%） | 明确定位为**辅助专家复核** |
| OpenAI SWE-Bench Pro 审计 | 自动筛选 + **每题五名工程师独立复核** |

**没有人的头条结果是"我们有一个骗不过去的机器确认机制"。** 这才是 BenchAudit 独有的东西 —— 我们的确认合同经过 19 项对抗攻击（篡改证据、交换方向、超时、去掉签名），全部降级为 review，一次都没有被骗出假 confirmed。

**一个更好的候选排序器，不会改变系统能"证明"什么。确认合同会。**

而你这轮跑完 100 题，**confirmed 层是 0**。这不是你的问题 —— 是这条路线本身产不出 confirmed。

---

## 三、好消息：新方向的线索在你自己的数据里

看你 §B 和 §C 里这一组题：

| item | 缺陷 | 现状 |
|---|---|---|
| chal-513 | David 做了 38 个，比 Zachary 多 56 个 → Zachary = **−18** | **BenchAudit 漏检** |
| chal-797 | 系列共 17 部电影，却"看了 21 部" | **BenchAudit 漏检** |
| chal-826 | 系列共 4 本书，却"读了 19 本" | **BenchAudit 漏检** |
| chal-417 | 71 − sold + 38 = 116 → sold = **−7** | 找到了 |
| chal-463 | 花瓶里 15 朵，扔掉 33 朵 | 找到了 |
| chal-908 | 有 9 块甜饼干，吃了 36 块 | 标 clean，你怀疑漏标 |
| chal-666 | 有 $4，花了 $13 | 标 clean，你怀疑漏标 |
| chal-390 | 买 200 瓶水，每天喝 312 瓶 | 标 clean，你怀疑漏标 |

**这一整类是纯算术可判的** —— 存量下界、消耗不超库存、计数非负。**完全不需要 LLM。**

而且注意：BenchAudit 相比单次 LLM 漏掉的 4 题里，**513、797、826 这 3 题就是这一类**。

一个确定性检查器可以同时做到：

- 补上 3/4 的漏检；
- **零 API 调用**；
- 产出**可重放的证据**，够得着 `confirmed` 层，而不是又一批 review 候选；
- 是**框架级的贡献**（一个新的确定性 checker），不是 SVAMP 调参。

**这就是你下一阶段要做的事。**

---

## 四、下一阶段的安排

按阶段推进，不按天。**每完成一个阶段就说一声，卡住了也随时说 —— 卡住本身也是信息。** 整体节奏大概一周多，但如果第 1 步就发现有意思的东西（比如回退原因是个真 bug），停下来把它挖清楚比赶进度重要。

### 阶段 0：同步代码（必须最先做，别跳过）

你现在在 `c6f62ce`（7-25）。7-27 之后主线加了一整套东西你都没有：

- 确认合同（`DifferentialOracleAuditChecker`）
- 中央 promotion 的独立重放
- execution attestation（分离 worker 签名 + 父进程验签）
- review-only 天花板与 fail-closed 策略

**这一步不做，你写的任何新 checker 都只能产 candidate，产不了 confirmed —— 等于白写。**

做法：

1. 同步到 `research/generalized-confirmation-metamorphic-20260727` 或其后继分支；
2. **先把那 5 个未提交改动 `git stash` 存好，别丢**（阶段 1 要用）；
3. 跑通全量测试（现在应该是 780+ passed）；
4. 读一下 `benchcore/differential_oracle.py` 和 `benchcore/promotion.py` 里 `_differential_oracle_replay` 那一段 —— 这是"什么才算 confirmed"的权威定义，你后面要接上它。

**完成标志**：全量测试通过，你能说清楚一个 finding 要满足哪些条件才能从 review 升到 confirmed。

---

### 阶段 1：让这轮实验变得可复现

这是你现在最有价值的一件事，而且**几乎不花 API**（cache 已有 662 条，命中就是零调用）。

要回答的问题：**F1 从 0.914 掉到 0.727，是代码改动造成的，还是模型换了造成的？**

做法：

1. 在**干净的 commit** 上，用**同一份 cache 和同一个 manifest** 重跑；
2. 和脏工作树的结果对比：
   - **结果不同** → 逐个还原那 5 个改动，定位是哪一个。这本身就是一个值得写下来的结论（很可能是个真 bug）；
   - **结果相同** → 回退来自模型侧（`deepseek-v4-flash` vs 历史模型）。这需要 API，**先来找我定预算再跑**；
3. 产出一份升级版环境记录：**干净 commit + 输入 SHA-256 + cache SHA-256 + 模型标识 + 完整命令**。

**完成标志**：任何人拿到这份记录，能跑出一模一样的数字。

**为什么重要**：现在这份误差分析，如果底下的构建是坏的，那分析的可能是人为制造的 bug。基线不可复现，后面所有改进都无法归因。

---

### 阶段 2：把两个缺失的数字补进主表（很快，但很重要）

**(a) 成本。** 你的 cache 是 662 行 / 100 题 = **每题 6.6 次 LLM 调用**，而单次 LLM 基线是 1 次。

也就是说：**BenchAudit 用 6.6 倍的成本，换来 F1 +0.036。**

这个数字必须进 `SVAMP_RESULTS.md` 主表。不写它，"BenchAudit 更好"的结论是不完整的 —— 而且它正好呼应你 §F 里"多个 auditor 重复同一根因 10 题"：重复触发既是噪声，也是钱。

**(b) FP 分层。** 把你 §C 里已经做好的裁决提到主表：

```
FP = 18，其中
  真正误报            10
  争议但证据不足        4
  Platinum 可能漏标     4
→ 真实 Precision 在 0.640 到 0.720 之间
```

你已经做完这个分析了，只是留在了误差分析文档里。**把它放到主表，因为它改变结论的读法。**

**完成标志**：主表里有成本列，Precision 有区间而不是单点。

---

### 阶段 3：冻结「确定性数量可行性检查器」协议 + 预飞

这是新方向的正式起点。**注意顺序：先写协议，再写代码。**

#### 3.1 先写协议（不写实现）

要在协议里定死：

1. **覆盖哪几类不可行性**，建议 v1 只做最硬的三类：
   - 消耗量 > 初始存量（chal-908、chal-390、chal-826、chal-797）
   - 推导出的计数为负（chal-513、chal-417、chal-463）
   - 花费 > 持有金额（chal-666）
2. **怎么从题面机械抽取「实体 — 初值 — 变化 — 终值」**，以及抽不出来时怎么办（**弃权，不猜**）；
3. **Go/No-Go 门槛**：预飞覆盖率达到多少才值得实现；
4. **控制集**：正常题必须零触发、方向交换必须不触发、抽取失败必须记 `NOT_IDENTIFIABLE` 而不是 clean；
5. **零 LLM 调用**。

#### 3.2 然后预飞（这一步能省掉大量白工）

**在写解析器之前，先在非目标数据上量一次可解析率。**

具体：在 SVAMP-Platinum **全量**（不只你那 100 题）上，跑一个**只统计、不判断**的扫描器，回答：

> 有多少题能机械抽出完整的「实体 — 初值 — 变化 — 终值」结构？

**这一步是有先例的**：我们前两天用同样的方法救了两次场 —— 一次两分钟的预飞省掉一整轮解析器开发，另一次几分钟的可得性检查省掉排了三周的工作。都是同一个形状：**在写领域适配器之前，先用聚合口径量一次可得性。**

如果覆盖率只有 3%，就别写了，写下来"V1 语法不成立"本身就是一个有价值的结论。如果有 30%，直接干。

**完成标志**：协议冻结成一个独立提交；预飞结果是一份带哈希的聚合 receipt（**不输出题目原文和 item ID**，只出计数）。

**这一步做完先别往下走，来找我复核协议。**

---

### 阶段 4：实现（只有预飞过关才做）

- 实现检查器 + 完整测试（每个 fail-closed 分支都要有 fixture）；
- **必须走中央 promotion**，不能自己判 confirmed；
- 控制集全部为零才允许升级；
- 在你那 100 题上重放，看能不能捞回 513、797、826。

**完成标志**：至少一条 SVAMP 缺陷达到 `confirmed`，且所有控制为零。

如果做到这一步 —— **那是这个项目第一次在数学题 benchmark 上产出 confirmed**，含金量比 F1 涨几个点高得多。

---

### 阶段 5：你原本提的字段一致性校验（放在最后）

你 §G 提的那个方向 **是对的**，我保留它，只是优先级在后面：

- 各字段一致 → 继续；
- 不一致 → 视为无效响应并重试；
- 重试仍不一致 → 降级 `unknown`，**不生成 candidate**。

这完全符合项目的 fail-closed 纪律。162/599/687/934/974 这 5 题写成固定回归测试也是对的做法。

放在后面的原因只有一个：**它是在改善 review 候选的质量，而 review 候选不是我们的主线**。等前面几步做完，如果还有时间，这一项做起来很快。

---

## 五、这段时间明确不要做的事

- 调阈值来降 FP（我们已经证明置信度饱和，这条路不存在）
- 加更多 auditor（现在每题已经 6.6 次调用了）
- 改 Platinum 标签让数字好看（你自己已经拒绝过一次，保持住）
- 为了凑数量放宽 confirmed 的判据
- 在脏工作树上跑任何要写进报告的实验

---

## 六、关于节奏

上面五个阶段，阶段 0–2 应该比较快，阶段 3 是重头。**不用卡时间**，但有两个请你主动同步的节点：

1. **阶段 1 做完**（知道回退原因了）—— 这个结论我想早点看到；
2. **阶段 3 的协议写完、预飞跑完** —— 实现之前我要复核一遍协议。

中间任何时候有进展、有发现、或者卡住了，都直接说。**特别是如果预飞结果是"覆盖率太低，不该做"—— 那是一个好结果，不是失败**，我们前两天就靠这种结论省掉了几周。

最后一句：你这轮的问题是方向，不是能力。**记录规范、口径清楚、逐条裁决、拒绝走捷径 —— 这四样是做研究最难教的部分，你已经有了。** 剩下的是把它们用在对的地方。
