 Claude 这次的踩刹车是对的。我还要再补两条对自己原型更严格的审计：初版 TF‑IDF 是在全部 1000 题上拟合后再做 subject folds，虽然没读标签，但属于 transductive 特征拟合；并且检索文本里显式加入了 subject 名称。这两点都不会制造随机标签下的高分，却会让“跨 subject”结果比严格 inductive 条件更乐观。因此这组
  数字只能保留为探索性信号，不能进入主结论。我会把当前未提交的四层实现继续冻结，不往前搭。

• Updated Plan
  └ 接受同行评审并进一步收紧：MMLU 原型属于未预注册、同 benchmark、transductive TF-IDF 且含 subject 文本的探索结果，不作为证据。暂停四层实现，先做最小 PrototypeRecord 与零 API leave-one-benchmark-out 检索可行性；不通过即停止。
    ✔ 核对本地分支、远端 GitHub 与 CI，确定最佳代码是否已推送
    □ 冻结最小缺陷原型 schema、LOBO 数据隔离规则和成功/停止门槛
    □ 构建不含目标 benchmark 的小型原型库并运行零 API LOBO 检索试验
    □ 仅在 LOBO 有增益时实现等预算 A/D/F DeepSeek 消融
    □ 通过后再实现最小 shadow 接口、测试并提交；失败则撤回未提交代码

• Claude 这轮评审是正确的，而且它成功阻止了我们被一个“看起来很漂亮”的探索结果带偏。我接受它的核心裁决。

  ## MMLU 初步结果还要再降一级

  Claude 指出的两个问题成立：

  1. 这是同一 MMLU-Redux 内的 subject-held-out，不是 leave-one-benchmark-out。
  2. 最强增益来自文本相似性，而文本正是最依赖 benchmark 分布的特征。

  我再补充两个刚才原型里的限制：

  - TF-IDF 在全部 1000 题上拟合后才划分 folds。虽然没读标签，不属于标签泄漏，但属于 transductive 特征拟合，严格实验应该每折只在训练部分拟合。
  - 检索文本显式包含了 subject 名称。测试 subject 虽然整体未进入训练，但 high school、physics 等共享词可能携带领域和缺陷率信息。

  因此：

  > 0.452→0.700 和 0.587→0.760 只能证明“值得继续做一次更严格实验”，不能证明 Memory 有效，更不能证明缺陷原型能跨 benchmark 迁移。

  Claude 没有独立复核这些数字也是事实。代码和结果确实是我刚才运行得到的，但目前只有单作者执行、未预注册，不应该作为正式结果。

  ## 四层架构暂时不建

  这一点也接受。

  当前独立 worktree 中已经有：

  - benchcore/audit_memory.py
  - memory-shadow CLI 草稿
  - 12 个通过的单元测试

  但这些都没有 commit、没有 push。它们继续冻结，暂时不扩展。后续如果最小 LOBO 失败，这批代码直接撤回，不让仓库背负无效抽象。

  GitHub main 不受影响，目前仍是：

  35d931e Add review-only archived trace ingestion

  GitHub Actions 已通过。

  ## 调整后的最小 schema

  第一轮只需要一种记录：PrototypeRecord。

  prototype_id: evaluator_broadcast_blindness
  defect_family: evaluator_unsoundness

  applicability:
    artifact: evaluator
    domains: [code, data_science]
    required_capabilities: [execute_candidate]

  positive_cues:
    - scalar and one-element array are both accepted
    - evaluator uses broadcast-compatible numerical comparison

  exclusion_cues:
    - contract explicitly declares shape equivalence
    - task is property-based and intentionally permits multiple representations

  verifier_hint:
    - construct a shape-changing mutant
    - execute it against the live evaluator

  provenance:
    source: DS-1000
    status: reproduced
    citation: ...

  反例先作为 exclusion_cues 内联，verifier 先作为一个提示字段内联，不单独建设 Counterexample/VerifierRecipe 表。

  只有 LOBO 证明有效后，才考虑拆分成独立实体。

  ## 现在真正需要做的决胜实验

  Claude 说得对：

  > 同 benchmark 的 A–E 都可能很好看，但只有 LOBO 下的 D/F 能证伪或支持立项理由。

  建议分两阶段。

  ### 第一阶段：零 API 检索可行性

  先不把 Memory 塞进 LLM prompt，只测试：

  > 从其他 benchmark 建立的缺陷原型，能否在目标 benchmark 中正确召回相应问题？

  轮流留出：

   目标 benchmark    Memory 来源
  ━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   SVAMP-Platinum    GSM8K、MMLU、论文、我们的执行案例
  ────────────────  ───────────────────────────────────
   GSM8K-Platinum    SVAMP、MMLU、论文、执行案例
  ────────────────  ───────────────────────────────────
   MMLU-Redux        SVAMP、GSM8K、论文、执行案例

  必须排除：

  - 目标 benchmark 的所有题目；
  - Redux/Platinum 等同源修订版本；
  - 专门讨论目标 benchmark 缺陷的论文案例；
  - 近重复题目；
  - 目标标签和错误说明；
  - 目标 benchmark 名称及 subject 元数据。

  特征提取器也必须每折只在 Memory 来源数据上拟合。

  如果这一阶段没有稳定 lift，就停止，不调用 DeepSeek，也不建设 Memory 模块。

  ### 第二阶段：等预算 A/D/F

  只有第一阶段通过，才运行：

   组别    同等调用预算下的配置
  ━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   A       无 Memory 的探索审计
  ──────  ───────────────────────────────────
   D       只使用缺陷原型定向审计
  ──────  ───────────────────────────────────
   F       一部分定向审计 + 一部分无先验探索

  为了真正等预算，可以固定每题两个视角：

  A：2 个无 Memory 视角
  D：2 个 Memory 定向视角
  F：1 个 Memory 定向视角 + 1 个无先验视角

  这样 F 的优势不能被解释成“只是多调用了一次模型”。

  B 和 C 不是消失，而是从主试验降为诊断：

  - B：taxonomy-only，判断是不是加几个缺陷名就够了；
  - C：具体错题实例检索，判断原型是否真的优于查相似题。

  ## 建议预注册的成功门槛

  第一阶段至少满足：

  1. LOBO 下原型检索优于随机原型和固定 taxonomy；
  2. 在至少两个目标 benchmark 上提高 Recall@固定复核预算；
  3. 提升不能完全来自同义词或 benchmark 身份信息；
  4. 随机标签/随机原型对照回到接近随机水平；
  5. 新缺陷家族不能因为原型检索被系统性压低；
  6. 结果经过按题 bootstrap，不能只看单个点估计。

  第二阶段至少满足：

  1. F 在相同调用数下优于 A；
  2. F 的已知家族召回提高；
  3. F 的未知家族召回不显著低于 A；
  4. Precision 不出现不可接受的下降；
  5. 改进在不止一个 benchmark 上出现。

  ## 最终裁决

  现在不应该继续搭 Memory 系统，应该先做一个最小、零 API、严格隔离的 LOBO 决胜实验。

  如果它失败：

  > 说明缺陷原型不具备足够的跨 benchmark 迁移性，Memory 最多适用于同 benchmark 的持续审计，不应成为通用 BenchAudit 的核心模块。

  如果它成功：

  > 才有理由建设 provenance、反例、verifier 和 shadow 接入，并运行等预算 DeepSeek 消融。

  这个顺序更便宜、更简单，也最符合我们项目一直强调的“先证伪核心假设，再建设系统”。