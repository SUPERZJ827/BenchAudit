# 反事实 LLM Judge 课题：现有工作差异与顶会可行性评估

> 更新时间：2026-07-26  
> 目的：判断“利用反事实关系评估 LLM Judge 可靠性”是否过于简单、与已有工作是否重复，以及需要升级成什么形式才具有顶会竞争力。

---

## 1. 结论先行

### 1.1 对基础版本的判断

如果课题只是：

1. 对回答进行格式改写、同义改写或定向破坏；
2. 再让 LLM Judge 评分；
3. 统计评分是否变化；
4. 将分数变化输入 Logistic Regression，预测 Judge 是否错误；

那么它确实：

- 实现简单；
- 与 2024—2026 年已有工作高度相邻；
- 容易被审稿人评价为“metamorphic testing、prompt sensitivity 和 selective evaluation 的直接组合”；
- 更像分析论文、应用论文或 workshop 论文，暂时不足以支撑 ICLR 主会。

### 1.2 仍然可能成立的顶会方向

更值得研究的问题不是：

> Judge 在反事实修改后会不会改变分数？

而是：

> 反事实关系本身无法识别一个“稳定犯错”的 Judge；那么，能否用大量廉价但带噪的关系约束，加上少量人工或客观锚点，实现跨 Judge、跨任务的标签高效可靠性校准？

建议将课题升级为：

> **Relations Are Not Enough: Label-Efficient Calibration of LLM Judges with Noisy Counterfactual Constraints**

中文可以表述为：

> **关系一致性并不等于判断正确：基于带噪反事实约束的 LLM Judge 标签高效校准**

这个版本的核心不再是“做几个扰动”，而是：

1. 说明为什么纯关系监督不足以判断正确性；
2. 建立带不确定性的关系约束模型；
3. 研究这些关系能否减少人工校准标签；
4. 在相同 Judge 调用预算下，验证其选择性评测价值。

---

## 2. 原始想法具体做什么

对每个评价实例：

```text
任务 x + 回答 y + rubric r
                ↓
          Judge 原始裁决
                ↓
     构造若干反事实版本 T(z)
                ↓
        Judge 对反事实重新裁决
                ↓
        得到 response profile
                ↓
      估计原始裁决是否可信
```

反事实关系主要包括：

| 关系 | 操作 | 预期 |
|---|---|---|
| Invariance | 只改变格式、排列或等价表达 | Judge 裁决基本不变 |
| Monotonicity | 删除必要内容、加入事实错误 | Judge 分数按已知方向下降 |
| Locality | 只破坏一个 rubric 维度 | 主要影响对应维度 |
| Preference consistency | 保持答案质量关系但交换位置 | 偏好方向不应无理由翻转 |

这些关系可以很便宜地发现 Judge 的异常行为，但存在一个重要边界：

```text
关系违反  ⇒ Judge 至少不稳定或对错误因素敏感
关系满足  ⇏ Judge 的原始裁决正确
```

---

## 3. 为什么基础版本显得简单

### 3.1 代码并不难

基础实现主要是：

- transformation generator；
- Judge API 调用；
- 分数差和翻转率统计；
- 一个二分类器；
- accept/defer 阈值。

这不是复杂的算法问题。一个质量合格的 pilot 在两周内可以实现。

### 3.2 更严重的问题是研究组件大多已有

以下单个贡献都已有直接相邻工作：

- 语义等价 paraphrase 下的 Judge 稳定性；
- rubric 等价改写下的不变性；
- 使用受控退化自动得到偏好方向；
- 定向破坏单个评分属性；
- 自动发现未知 Judge 偏差；
- 使用置信度决定 accept/escalate；
- 使用 conformal prediction 估计单样本风险；
- 使用 hidden-state linear probe 校准 Judge。

因此，“把这些组件放到同一个系统中”不自然等于新的方法贡献。

### 3.3 顶会不要求代码复杂，但要求结论新

简单方法不是原罪。Logistic Regression 也可能支撑顶会论文，前提是它证明了此前未知的重要结论，例如：

> 在相同调用预算下，反事实关系能够在未见过的 Judge 和任务上预测 Judge-human disagreement，并将达到相同风险水平所需的人类标签减少一半。

如果只能得到：

> 等价改写会让 Judge 的评分发生变化。

那么无论工程做得多复杂，新颖性都不足。

---

## 4. 与现有工作的逐项比较

### 4.1 JudgeSense：语义等价提示的稳定性

[JudgeSense](https://arxiv.org/abs/2604.23478)构造了经过验证的 paraphrase pairs，并用 Judge Sensitivity Score 衡量 Judge 在等价提示下是否保持相同决定；论文评测了 9 个 Judge 和 494 组 paraphrase。

| 比较项 | JudgeSense | 我们的基础想法 |
|---|---|---|
| 等价改写 | 有 | 有 |
| Judge 翻转率 | 有 | 有 |
| 多 Judge 比较 | 有 | 计划有 |
| 定向质量退化 | 不是重点 | 有 |
| 预测原始 verdict 错误 | 没有作为核心 | 计划有 |
| Accept/defer | 没有 | 计划有 |

差异空间：

> 不能再把“paraphrase 稳定性”当作主要贡献，只能把它作为一种 probe。

### 4.2 Policy Invariance：Rubric 改写不变性

[Beyond Accuracy: Policy Invariance as a Reliability Test for LLM Safety Judges](https://arxiv.org/abs/2605.06161)研究：

- 语义等价的 policy/rubric 改写；
- 有意义的 strict-to-lenient 修改；
- Judge 能否区分无意义形式变化和真实规范变化。

论文报告等价 policy 重写会引起额外 verdict flips，并提出 Policy Invariance Score 和 Judge Card。

| 比较项 | Policy Invariance | 我们的基础想法 |
|---|---|---|
| Rubric 等价改写 | 核心贡献 | 属于 invariance probe |
| 定向规范变化 | 有 | 有类似 monotonicity |
| Agent/safety trajectory | 有 | 可能扩展 |
| 单样本错误概率 | 不是核心 | 计划作为核心 |
| 标签效率 | 不是核心 | 可成为差异点 |

差异空间：

> “改 rubric 看 Judge 是否翻转”已经不足以构成新贡献，必须证明这些响应能预测原始 verdict 风险。

### 4.3 BabelJudge：通过质量退化自动生成金标签

[BabelJudge](https://arxiv.org/abs/2606.22329)从高质量回答出发，通过受控退化构造已知偏好方向，测试位置偏差、长度偏差、顺序不一致和跨语言退化，并扩展到 agent trajectory。

| 比较项 | BabelJudge | 我们的基础想法 |
|---|---|---|
| 受控质量退化 | 核心 | 核心 |
| 无人工构造方向标签 | 有 | 有 |
| Agent trajectory mutation | 有 | 计划可能有 |
| 多语言 | 有 | 非必须 |
| 预测普通原始样本是否误判 | 不是核心 | 计划作为核心 |
| 少量人工锚点校准 | 不是重点 | 可成为核心 |

差异空间：

> “从好回答构造坏回答，再检查 Judge 是否识别”已经被直接覆盖。需要研究这些 probe 对自然分布中原始 verdict 错误的预测能力，而不是只测合成退化识别率。

### 4.4 Attribute-Level Compositionality：定向属性退化

[What makes the whole? Probing Attribute-Level Compositionality in LLM Judges](https://openreview.net/forum?id=xd9qriSZQh)研究多维 Judge 是否真正按各属性组合整体分数，并测试其对 targeted trait degradation 的因果响应。

| 比较项 | Attribute Compositionality | 我们的基础想法 |
|---|---|---|
| 多维 rubric | 有 | 有 |
| 定向破坏单一属性 | 有 | 有 |
| Locality/compositionality | 核心 | 原计划核心之一 |
| 预测 verdict 是否错误 | 不是核心 | 计划有 |
| 选择性接受 | 没有 | 计划有 |

差异空间：

> Locality 不能作为主要新贡献，最好降为一类辅助 probe。

### 4.5 BiasScope：自动发现未知偏差

[BiasScope](https://openreview.net/forum?id=QGOw6AU8Lp)使用 LLM 驱动的迭代流程自动发现潜在 Judge 偏差，并通过扰动测试验证这些偏差。

| 比较项 | BiasScope | 我们的基础想法 |
|---|---|---|
| 自动发现新偏差 | 有 | 暂无或可扩展 |
| 偏差库 | 有 | 可有 probe bank |
| 扰动验证 | 有 | 有 |
| 单样本可靠性预测 | 非主要目标 | 核心目标 |
| 风险控制 | 非主要目标 | 计划有 |

差异空间：

> 不宜把“自动生成新的 transformation”作为主要卖点，除非能够证明新 probe 对错误预测的边际信息增益。

### 4.6 TrustJudge：评分与偏好的内部一致性

[TrustJudge](https://iclr.cc/virtual/2026/poster/10011516)研究：

- 单项评分和 pairwise comparison 的不一致；
- 偏好传递性违反；
- 概率化评分和聚合。

差异空间：

> TrustJudge 的一致性主要发生在不同评价协议之间；我们的潜在差异是对同一实例做受控干预，并预测该实例的 verdict 风险。

但 TrustJudge 式 consistency 必须成为基线，不能被忽略。

### 4.7 Trust or Escalate：选择性评测

[Trust or Escalate](https://arxiv.org/abs/2407.18370)已经研究何时接受 Judge、何时升级给更强 Judge 或人类，并提供与人类一致性的风险保证。

| 比较项 | Trust or Escalate | 我们的基础想法 |
|---|---|---|
| Accept/escalate | 核心 | 计划有 |
| 置信度估计 | 有 | 有 |
| 风险保证 | 有 | 计划使用 |
| Counterfactual profile | 没有作为主要信号 | 潜在核心差异 |

差异空间：

> Accept/defer 不能作为创新本身。需要证明反事实 profile 是比已有置信度估计更有效的新风险信号。

### 4.8 Per-instance reliability 与 conformal prediction

[Diagnosing LLM Judge Reliability](https://openreview.net/forum?id=FbDuesI6tD)使用传递性违反和 conformal prediction set 分析单样本 Judge 可靠性。

[Calibrating LLM Judges: Linear Probes for Fast and Reliable Uncertainty Estimation](https://aclanthology.org/2026.acl-industry.14/)则直接使用 Judge 内部表征训练线性 probe 估计不确定性。

差异空间：

> “预测单样本是否可靠”本身也不是空白。必须证明反事实关系在黑盒 Judge、跨模型迁移和少样本校准中具有独特优势。

---

## 5. 重复程度总结

| 原计划组件 | 是否已有相邻工作 | 重复风险 |
|---|---|---:|
| 等价 paraphrase 不变性 | 已有直接工作 | 很高 |
| Rubric 等价改写 | 已有直接工作 | 很高 |
| 定向质量退化 | 已有直接工作 | 很高 |
| Locality/属性组合 | 已有直接工作 | 很高 |
| 自动发现偏差 | 已有直接工作 | 很高 |
| 多次采样和一致性 | 已有大量工作 | 很高 |
| 单样本风险预测 | 已有工作 | 中高 |
| Accept/defer | 已有带保证方法 | 很高 |
| Counterfactual profile 预测自然样本错误 | 有相邻工作，但尚有空间 | 中 |
| Counterfactual 关系减少人工校准标签 | 尚有相对清楚的空间 | 中低 |
| 带噪关系的可识别性与最小锚点 | 更接近潜在新问题 | 较低 |
| 未见 Judge/任务/变换的关系校准泛化 | 尚需实验证明 | 中低 |

因此，原始方案中真正值得保留的不是 transformation bank 本身，而是：

> **关系证据能否减少获得可靠 per-verdict 风险估计所需的人类监督。**

---

## 6. 基础方案的根本理论缺陷

假设某个 Judge 系统性偏爱“语言流畅但事实错误”的长答案。

它可能对原答案给 9 分，并且在以下操作后仍然给 9 分：

- 格式改写；
- 句子重排；
- 同义改写；
- 选项重新编码；
- 删除无关元数据。

那么该 Judge 满足 invariance，但原始评分仍然是错的。

因此：

\[
\text{Relational consistency} \neq \text{Human-aligned correctness}
\]

更一般地说，只依赖 equality 和 order constraints 时，可能存在多个 Judge：

- 对所有关系都给出相同的响应；
- 但与人类评价的绝对方向完全不同。

这意味着：

> 纯反事实关系最多能提供相对行为约束，不能在没有任何外部锚点时识别人类意义上的正确性。

这不是需要隐藏的缺陷，反而可以成为更强论文的理论出发点。

---

## 7. 推荐升级：带噪关系约束的标签高效校准

### 7.1 重新定义研究问题

不再声称：

> 无需人类标签，通过反事实关系即可知道 Judge 是否正确。

改为：

> 使用少量人工或客观锚点时，大量廉价的反事实关系能否显著提升 Judge 风险估计，并减少达到目标风险所需的人工标签？

### 7.2 关系图表示

将原始回答和反事实回答表示为图节点：

```text
                         ┌─ 格式等价版本
                         │
原始回答 ── invariance ──┼─ 同义改写版本
    │                    │
    ├── monotonicity ────┴─ 删除必要内容
    │
    └── locality ────────── 制造单维事实错误
```

边记录：

- 关系类型；
- 预期方向；
- transformation 来源；
- transformation validity confidence；
- 是否由确定性 verifier 支持；
- 是否经人工抽检。

### 7.3 处理 transformation 本身的噪声

基础版本默认 transformation 正确，这是不可靠的。

升级版本应显式建模：

\[
P(\text{relation valid})
\]

确定性变换可以赋予高权重；LLM 生成且只经过另一个 LLM 判断的变换权重较低；人工确认的隐藏子集用于校准这些权重。

这样可以区分：

```text
Judge 违反关系
```

和：

```text
所谓“等价改写”其实改变了答案质量
```

### 7.4 少量锚点

锚点来源包括：

- 人类评分；
- 多人偏好分布；
- 数学确定性答案；
- 代码单元测试；
- instruction-following 的确定性约束；
- grounded citation verifier。

实验模拟不同锚点预算：

```text
25 / 50 / 100 / 200 / 500 条
```

比较达到相同风险所需的标签量。

### 7.5 方法可以保持简单

第一阶段：

- Logistic Regression；
- Gradient Boosting；
- 小型 MLP；
- calibration；
- conformal risk control。

如果简单模型已经稳定有效，说明增益来自关系证据，而不是模型容量。

信号确认后，再考虑：

- graph regularization；
- probabilistic relational model；
- cost-aware probe selection。

---

## 8. 一个更有顶会辨识度的贡献组合

建议论文最终只保留四项贡献。

### Contribution 1：可识别性边界

形式化说明关系一致性为什么不能单独恢复人类正确性，以及最少需要什么形式的外部锚点。

### Contribution 2：Noisy Relational Reliability Model

提出带 transformation validity 的关系风险估计方法，而不是把所有 counterfactual edge 当作绝对正确。

### Contribution 3：Label-Efficient Calibration

证明在相同风险下，关系证据减少人工校准标签；或者在相同标签预算下，提高 selective evaluation 的覆盖率。

### Contribution 4：Cross-Generalization Protocol

在以下设置中冻结评估：

- unseen judge family；
- unseen task family；
- unseen transformation family。

其中 unseen transformation 最关键，因为它决定方法是在学习一般可靠性，还是只记住几种扰动模板。

---

## 9. 实验如何设计才不被认为“只是多调了几次 API”

### 9.1 等调用预算

假设每个样本允许额外调用 Judge 四次，必须比较：

1. 同一输入重复采样四次；
2. 四个随机扰动；
3. 四个固定已知偏差 probe；
4. 四个 counterfactual relation probe；
5. 四个不同 Judge 投票；
6. 自适应选择四个 probe。

所有方法：

- Judge 调用次数相同；
- transformation 生成成本单独计入；
- 不允许 counterfactual 方法因为调用更多而获得不公平优势。

### 9.2 防止数据泄漏

必须按原始 item 切分：

```text
原始回答及其所有变体只能出现在同一个 split。
```

还需要：

- transformation generator 与目标 Judge 模型家族隔离；
- 不允许 target human label 参与 probe 选择；
- 文本相似的原题不能跨 train/test；
- 按任务和 Judge 家族做真正 holdout。

### 9.3 统计单位

一个原始样本可能产生多个变体，但它们不是独立样本。

因此：

- bootstrap 应按原始 item 聚类；
- 置信区间不能把 1 个原题的 6 个变体当成 6 个独立观测；
- 主结果报告跨随机种子和跨 Judge family 的区间。

---

## 10. 主指标应该是什么

### 10.1 错误预测

- AUPRC：类别不平衡时优先于 AUROC；
- AUROC；
- Brier score；
- Expected Calibration Error。

### 10.2 选择性评测

- Risk–Coverage Curve；
- AURC；
- 50%/70%/80%/90% coverage 下的错误率；
- worst-task risk；
- worst-judge risk。

### 10.3 标签效率

这是升级版最重要的指标：

- 达到 5% selective risk 需要多少人工标签；
- 使用 50/100/200 个锚点时的 AURC；
- 相对无关系约束基线节省多少标签；
- 新 Judge 上只用多少校准样本即可恢复目标风险。

### 10.4 成本

- 每个样本平均 probe 数；
- 每个被接受 verdict 的调用成本；
- 每发现一个错误所需调用数；
- 达到目标风险的总成本。

---

## 11. 两周生死 Pilot

不要先实现完整关系图模型。先确认反事实关系是否真的包含新增信息。

### 11.1 最小规模

- 3 个任务族；
- 每类 150–200 个原始样本；
- 4 个不同模型家族的 Judge；
- 2 类 invariance；
- 2 类 monotonic degradation；
- 每个样本固定 4 个 probes；
- 使用现有人工标签或确定性 verifier；
- Logistic Regression 和 Gradient Boosting。

### 11.2 必须比较

- Judge 原始 margin；
- self-reported confidence；
- 等成本重复采样；
- multi-judge disagreement；
- counterfactual profile；
- counterfactual + 其他特征。

### 11.3 Go / No-Go 门槛

建议至少同时满足：

1. 等预算 AUPRC 提升约 5 个百分点；
2. 80% coverage 下错误率相对下降至少 20%；
3. 至少 3 个 Judge 家族方向一致；
4. unseen transformation 上仍有收益；
5. 相同 AURC 所需人工锚点明显减少；
6. 按原始 item 聚类 bootstrap 后区间排除零。

如果只能在一个 Judge、一个任务或训练时见过的 transformation 上提升，应立即停止或降级为分析工作。

---

## 12. 三种版本的投稿潜力

| 版本 | 核心内容 | 新颖性 | 顶会潜力 |
|---|---|---:|---:|
| V0：扰动测试 | 改写/退化后统计 Judge 翻转 | 低 | 低 |
| V1：Profile 分类 | 反事实特征预测 Judge-human disagreement | 中 | 中，需极强实验 |
| V2：关系校准 | 非可识别性 + 带噪关系 + 少量锚点 + 跨域风险控制 | 中高 | 有机会 |

### V0 的典型审稿意见

> Similar perturbation-based diagnostics have already been studied; the contribution is mainly an aggregation of known tests.

### V1 的典型审稿问题

> Is the gain merely due to additional judge calls, task difficulty, or transformation-specific leakage?

### V2 需要回答的问题

> Do counterfactual relations reduce the amount of human supervision required for calibrated per-verdict reliability on unseen judges and tasks?

这个问题更难，但也更明确、更有论文价值。

---

## 13. 与 BenchAudit 现有工作的关系

这个新课题不需要继承 BenchAudit 的全部代码，但可以复用其方法论资产：

- identical-output control；
- deterministic mutation；
- relation evidence 分级；
- transformation provenance；
- review-only / fail-closed 原则；
- 等预算配对实验；
- 冻结 holdout；
- cluster bootstrap；
- 负结果保留机制。

BenchAudit 最有帮助的不是现有 checker，而是过去已经建立的实验纪律：

> 不能把同一逻辑的重复执行当作独立证据，也不能把人工共设计的 transformation 上的满分当作任意场景泛化。

---

## 14. 最终建议

### 是否过于简单？

基础版本是，升级版本不是。

### 是否与现有工作重复？

基础组件高度重复。真正相对空缺的是：

- counterfactual profile 对自然分布中原始 verdict 错误的增量预测价值；
- 它能否减少人工校准标签；
- 它能否迁移到未见 Judge、任务和 transformation；
- 如何在 transformation 本身带噪时进行可靠估计。

### 是否值得立即投入完整开发？

不值得。应先做两周等预算、严格切分的 pilot。

### 什么结果出现后才值得冲击 ICLR？

至少需要证明：

> 反事实关系不是简单的不稳定性指标，而是一种能够跨 Judge 泛化、减少人工锚点需求、并改善 selective risk 的独立信息源。

如果 pilot 只证明“格式变化会影响 Judge”，应停止这个方向。

如果 pilot 证明：

- 等成本显著优于重复采样；
- unseen Judge 和 unseen transformation 仍然有效；
- 相同风险下人工标签需求明显下降；

那么这个方向具备继续发展为 ICLR/ICML 级课题的理由。

---

## 15. 参考工作

1. [TrustJudge: Inconsistencies of LLM-as-a-Judge and How to Alleviate Them](https://iclr.cc/virtual/2026/poster/10011516)
2. [BiasScope: Towards Automated Detection of Bias in LLM-as-a-Judge Evaluation](https://openreview.net/forum?id=QGOw6AU8Lp)
3. [JudgeSense: A Benchmark for Prompt Sensitivity in LLM-as-a-Judge Systems](https://arxiv.org/abs/2604.23478)
4. [Beyond Accuracy: Policy Invariance as a Reliability Test for LLM Safety Judges](https://arxiv.org/abs/2605.06161)
5. [BabelJudge: Measuring LLM-as-a-Judge Reliability Across Languages and Agent Trajectories](https://arxiv.org/abs/2606.22329)
6. [What makes the whole? Probing Attribute-Level Compositionality in LLM Judges](https://openreview.net/forum?id=xd9qriSZQh)
7. [Trust or Escalate: LLM Judges with Provable Guarantees for Human Agreement](https://arxiv.org/abs/2407.18370)
8. [Diagnosing LLM Judge Reliability: Conformal Prediction Sets and Transitivity Violations](https://openreview.net/forum?id=FbDuesI6tD)
9. [Calibrating LLM Judges: Linear Probes for Fast and Reliable Uncertainty Estimation](https://aclanthology.org/2026.acl-industry.14/)

