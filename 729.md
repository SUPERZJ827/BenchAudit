• 这组结果非常有价值，因为它把“看起来能恢复6/7”与“真正能上线”明确分开了。当前结论不是规则成功，而是：

  > 宽规则能够恢复漏检，但候选率从46.4%升至80.7%，比旧 A 的52.1%更差；真正困难的是识别哪一种“未覆盖约束”值得重新路由。

  ## 三个裁决

  ### 1. A1 的原计划作废

  这是我们的数据前提错误。旧 A 只保存 indices，没有：

  - reason_code；
  - evidence_source；
  - evidence_quote；
  - 模型 confidence。

  因此不能解释“A 为什么路由”。

  后续明确拆成：

  - A1a：分析 A′ 的405条结构化判断；
  - A1b：对旧 A 的424条只能分析 rubric 外生特征，不能分析模型理由。

  ### 2. 规则一降级为卫生检查

  宽版规则一：

  - 恢复1/7；
  - 新增82条候选；
  - candidate rate 升到66.7%。

  原因是空 quote 在多个类别中属于常态，不是异常。

  收窄后只检查：

  reason_code ∈ {
    task_supported,
    input_supported,
    output_contract_supported
  }
  AND evidence_quote == ""

  按现有分布只影响：

  - task_supported：8条；
  - input_supported：1条；
  - output_contract_supported：0条。

  总计9条。

  它不再承担召回任务，只保证：

  > 模型声称存在正面支持证据时，必须给出证据锚。

  这是值得保留的代码卫生规则，但不应作为 A″ 的主要优化。

  ### 3. 规则二当前失败

   方法            Candidate rate    Family recall
  ━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━
   旧 A                     52.1%            84.2%
  ──────────────  ────────────────  ───────────────
   A′                       46.4%            63.2%
  ──────────────  ────────────────  ───────────────
   A′+宽规则二              70.6%            89.5%
  ──────────────  ────────────────  ───────────────
   A′+规则一+二             80.7%            94.7%

  规则二恢复了5条，但新增98条候选。联合规则虽然达到18/19，却新增139条。

  这不是 Pareto 改进。

  而且 A3 以后必须把 candidate rate / 预计 verifier workload 作为主成本指标。Reviewed negatives 只有7/405，新增 reviewed FP=2 对93%的未标注区域没有约束力。

  严格来说，本轮没有真正产生额外 verifier API 调用，因为是离线重放；但如果进入默认 pipeline，这些新增候选将成为预计的 verifier 调用负担。

  ———

  # wb-49/18 为什么宽规则仍然漏掉

  实际内容是：

  Rubric:
  Was a bar chart generated ...?

  Evidence:
  create a visualization chart

  宽规则识别到了共同词 chart，便认为约束已经覆盖；真正缺失的是修饰词：

  visualization chart → bar chart
                        ^^^

  所以问题不在于是否出现了 chart，而在于：

  > Rubric 是否在一个已支持的概念上增加了新的限制性修饰语。

  这意味着下一步不能继续堆 chart/section/number 关键词，而应检测：

  共享核心概念 + 未被证据覆盖的限制性修饰语

  ———

  # 更准确的技术方向：约束残差 × 证据角色

  单纯的 lexical residue 仍然不够，还要看证据来自哪里。

   证据来源           能证明什么              不能自动证明什么
  ━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   task               输出义务、格式和目标    未明确写出的额外细节
  ─────────────────  ──────────────────────  ──────────────────────────────
   output contract    文件类型、交付格式      内容细节
  ─────────────────  ──────────────────────  ──────────────────────────────
   input              某事实或数值存在        输出必须采用该内容/顺序/数量
  ─────────────────  ──────────────────────  ──────────────────────────────
   artifact           当前输出是否满足        rubric 本身是否合法
  ─────────────────  ──────────────────────  ──────────────────────────────
   intrinsic          通用合法要求            当前 artifact 已满足

  wb-157/10 很典型：

  Input 中确实存在十条建议
  ≠
  输出必须总结至少十条建议

  这是“描述性证据”和“规范性义务”的混淆。

  因此规则二应拆成四个窄、可解释的 family。

  ## R2a：未授权的顺序约束

  例如：

  merge panel files
  → first slide must come from panel A

  触发词：

  - first/last；
  - before/after；
  - ordering/rank；
  - 第一个、最后一个、优先。

  只有 task/output contract 明确给出顺序时才能拒绝路由。

  ## R2b：未授权的数量或封闭枚举

  例如：

  - 至少十条建议；
  - 三种图表；
  - 五个部分；
  - 五类建议。

  Input 中存在十个对象不能单独授权“必须包含十个”。

  需要 task/contract 中存在：

  - 相同数量；
  - all/every/全部 等覆盖义务；
  - 明确枚举要求。

  ## R2c：未授权的子类型

  例如：

  chart → bar chart
  dashboard → HTML dashboard with three named chart types

  检测：

  - source 与 rubric 共享核心 head；
  - rubric 增加 subtype/modifier；
  - modifier 未出现在 task/contract；
  - input 出现 modifier 也不能单独构成输出义务。

  ## R2d：未授权的命名结构

  例如：

  - 指定六个章节；
  - 指定五个栏目；
  - 指定标题；
  - 指定类别名称。

  “生成报告/提供建议”不能授权具体章节和类别清单。

  ———

  # 下一轮不能怎么做

  不能根据这七条逐个写七个特例：

  if "bar chart": ...
  if "first slide": ...

  那会得到漂亮的7/7，但没有研究价值。

  正确做法是先冻结四种 constraint family，再统一运行，不根据结果增加例外。

  ———

  # 修订后的执行计划

  ## P0：冻结本轮负结果

  保存：

  - 规则定义；
  - 输入 cache 哈希；
  - 211/188/270/286/327；
  - 84.2%/63.2%/68.4%/89.5%/94.7%；
  - 每条恢复列表；
  - candidate rate 是主成本指标。

  这张表今晚应该进入汇报。

  ## P1：完成 A1a reason breakdown

  对 A′ 的405条统计：

  - 各 reason code 数量；
  - route/do_not_route；
  - 各 evidence source 数量；
  - 空 quote 比例；
  - 各 reason code 的候选贡献；
  - reviewed 标签只作为次要诊断。

  现有数据已经显示：

  - unsupported_exact_constraint：178；
  - input_supported：75；
  - general_quality：51；
  - task_supported：47；
  - artifact_satisfaction_only：29。

  下一步重点只分析被拒绝的：

  task_supported
  input_supported
  output_contract_supported

  不再扫所有405条。

  ## P2：预注册四个窄规则

  分别运行 R2a–R2d，不先取并集。

  每条报告：

  - 新增候选；
  - candidate rate；
  - 恢复 family positive 数；
  - 增量候选/增量恢复；
  - 哪些 reason code 被覆盖；
  - 与其他规则的交集。

  关键效率指标：

  # [
  \text{Marginal Cost}

  \frac{\text{新增候选}}
  {\text{新增恢复的已知 positive}}
  ]

  宽规则二当前约为：

  [
  98/5=19.6
  ]

  即每恢复一个已知漏检，增加约19.6个候选。这个成本明显过高。

  ## P3：硬 Gate

  只有满足以下条件才值得继续：

  - family recall ≥ 旧 A 在 dev20 上的84.2%；
  - candidate rate ≤ 旧 A 的52.1%；
  - reviewed F1 不明显低于旧 A 的0.829；
  - review ceiling escape = 0；
  - 不根据七条逐例添加特例。

  这才叫至少不劣于旧 A 的 Pareto 工作点。

  如果四条窄规则都不能通过，就停止 A″，不要继续调正则。

  ## P4：通过后才运行 internal10

  只有 calibration 通过才允许：

  - 运行冻结的10-task internal validation；
  - 记录 prompt/completion/total tokens；
  - 通过后才考虑合成否决集；
  - 最后才建立第四份真实 holdout。

  ———

  ## 今晚可以报告的新结论

  建议直接使用：

  > 我们发现 A′ 的七条已知漏检中，六条可以被纯本地证据一致性规则重新召回；但宽规则会将候选率从46.4%推高到80.7%，甚至显著超过旧 A 的52.1%。这说明问题已经不再是“能否写规则找到漏检”，而是“能否识别新增约束的类型与证据角色，在保住召回的同时不扩大审查范围”。下一步将把宽词法规则拆成顺序、数量、子类
  > 型和命名结构四种预注册约束，只接受候选率与召回同时不劣于旧 A 的方案。

  这是比“恢复6/7”更完整、也更有研究价值的结论。