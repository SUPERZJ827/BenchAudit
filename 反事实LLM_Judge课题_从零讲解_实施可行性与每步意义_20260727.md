# 反事实 LLM Judge 课题：从零讲解、实施可行性与每一步的意义

> 更新时间：2026-07-27  
> 适合读者：第一次接触 LLM-as-a-Judge、反事实测试和选择性评测，希望真正理解“这个课题到底在研究什么、为什么值得研究、具体怎么做”的读者。  
> 本文不默认读者已经理解上一份研究方案。

---

## 0. 先用一句话说清楚这个课题

很多 benchmark 使用一个 LLM 给另一个模型的回答打分，但我们通常不知道：

> **这个 LLM Judge 在当前这道题上的评分究竟值不值得信任。**

本课题的做法是：

> 对同一个回答做少量“质量不变”或“质量按已知方向变化”的受控修改，观察 Judge 是否做出合理响应，再利用这些响应和少量人类/客观标签，预测原始评分是否可靠。

它研究的不是：

- 如何让模型回答得更好；
- 如何直接判断 benchmark 题目有没有错；
- 如何完全取代人工评价；
- 如何训练一个更大的 Judge。

它研究的是：

> **在没有能力人工复核全部样本时，怎样识别哪些 Judge verdict 可以接受，哪些需要进一步检查。**

---

## 1. 什么是 LLM Judge？

假设我们让一个模型回答：

> 请解释为什么地球有四季。

模型给出一个答案。过去可以让人类专家评分，但人工评价非常贵，所以现在经常让另一个 LLM 评分：

```text
任务：解释为什么地球有四季
回答：……
评分标准：
1. 是否提到地轴倾斜
2. 是否正确解释公转
3. 是否存在事实错误

请给回答打 1—5 分。
```

负责评分的模型就是 LLM Judge。

Judge 可能输出：

```json
{
  "score": 4,
  "reason": "回答基本正确，但没有充分解释南北半球季节相反。"
}
```

### 为什么大家要用 LLM Judge？

因为它：

- 比人工便宜；
- 比人工快；
- 可以批量处理几十万条回答；
- 能理解开放式自然语言；
- 能按照复杂 rubric 输出多维评分。

### 为什么 Judge 本身又不可靠？

它可能受到以下因素影响：

- 回答放在 A 还是 B；
- 回答长短；
- 格式是否漂亮；
- Judge prompt 的措辞；
- 生成回答的模型是否与 Judge 同源；
- rubric 是否复杂；
- 是否需要专业知识；
- 采样随机性；
- Judge 是否真正检查了任务要求。

因此，一个 benchmark 最后报告的分数，可能同时混入：

```text
被测模型能力
+ Judge 的随机误差
+ Judge 的系统性偏差
+ Rubric 表述方式
+ 评分协议设计
```

我们希望把“被测模型能力”和“Judge 自身问题”区分开。

---

## 2. 这个课题最直观的例子

### 2.1 原始任务

任务要求：

> 写一份销售分析，必须包含销售额、利润和同比变化。

模型回答：

```text
本季度销售额为 1000 万元，同比增长 12%；
利润为 180 万元，同比增长 8%。
增长主要来自华东地区的新客户。
```

Judge 给出：

```text
8 分：内容完整，分析基本合理。
```

问题是：我们不知道 Judge 为什么给 8 分。

它可能真的检查了：

- 销售额；
- 利润；
- 同比变化。

也可能只是看到文字流畅、数字丰富，就给了高分。

### 2.2 做一次质量不变修改

把段落改成项目符号：

```text
- 销售额：1000 万元，同比增长 12%
- 利润：180 万元，同比增长 8%
- 主要增长来源：华东地区新客户
```

内容没有变化，因此理想 Judge 的分数不应发生明显变化。

如果分数从 8 变成 5，说明 Judge 对格式过度敏感。

这叫：

> **Invariance Probe：质量不变，裁决应基本不变。**

### 2.3 做一次定向退化

删除利润信息：

```text
本季度销售额为 1000 万元，同比增长 12%。
增长主要来自华东地区的新客户。
```

现在回答明确漏掉了任务要求中的“利润”。

理想 Judge 应降低 completeness 分数。

如果仍然给 8 分，说明 Judge 可能根本没有检查利润要求。

这叫：

> **Monotonicity Probe：质量明确下降，裁决应按已知方向下降。**

### 2.4 只破坏一个维度

将销售额的“增长 12%”改成“下降 12%”，制造一个事实错误，但保持语言风格不变。

理想情况：

- factuality 明显下降；
- style 基本不变；
- completeness 基本不变。

如果 factuality、style、completeness 全部同时大幅下降，说明 Judge 给出的多维评分可能只是一个整体印象，并没有真正区分 rubric 维度。

这叫：

> **Locality Probe：只改变一个质量维度，其他维度不应无理由变化。**

---

## 3. 最终想得到什么？

不是简单得到一句：

> 这个 Judge 不稳定。

而是针对每个原始 verdict 得到一个行为 profile：

```json
{
  "original_score": 8,
  "repeat_std": 0.2,
  "format_delta": 0,
  "paraphrase_delta": -1,
  "omission_delta": 0,
  "factual_corruption_delta": -3,
  "locality_leakage": 2,
  "score_margin": 0.3
}
```

这个例子表示：

- Judge 重复打分很稳定；
- 不受格式影响；
- 能识别事实错误；
- 但没有识别“缺少必要内容”；
- 部分维度存在相互污染。

系统再输出：

```json
{
  "probability_original_verdict_is_wrong": 0.74,
  "action": "defer"
}
```

最终动作可以是：

| 动作 | 含义 |
|---|---|
| Accept | 当前裁决风险较低，直接采用 |
| Probe more | 信息不足，再运行一个有针对性的测试 |
| Defer | 风险较高，交给更强 Judge 或人工 |

---

## 4. 为什么不能直接让 Judge 多打几次分？

这是理解本课题最重要的一点。

### 4.1 重复采样能发现随机不稳定

同一答案运行三次：

```text
8 分、5 分、9 分
```

我们很容易判断 Judge 不稳定。

### 4.2 但重复采样发现不了“稳定犯错”

同一答案运行三次：

```text
8 分、8 分、8 分
```

看起来非常可信。

但是删除必需的利润内容后，Judge 仍然给：

```text
8 分、8 分、8 分
```

这说明它不是随机犯错，而是稳定地忽略某项要求。

重复采样测的是：

> Judge 是否每次都说同样的话？

反事实 probe 测的是：

> Judge 是否对真正影响质量的因素敏感，同时对无关因素不敏感？

两者不是同一种信息。

这也是本课题可能有价值的核心原因。

---

## 5. 但反事实关系也不能直接证明 Judge 正确

假设 Judge 系统性偏爱语言流畅的长答案，即使答案事实错误，它也稳定给高分。

我们做：

- 格式改写；
- 句子重排；
- 同义改写；
- 删除无关元数据。

Judge 始终稳定给 9 分。

它满足了所有 invariance，但它依然是错的。

因此：

\[
\text{关系一致性} \neq \text{裁决正确性}
\]

关系 probe 能发现：

- 对无关变化敏感；
- 对重要变化失明；
- 多维评分异常联动；
- 位置或格式偏差；
- 随机不稳定。

但它不能发现全部稳定、系统性的错误。

所以我们仍然需要少量“锚点”：

- 人类标注；
- 数学确定性答案；
- 代码单元测试；
- 确定性规则；
- 可验证的引用证据。

本课题的合理主张不是“完全不要人工”，而是：

> **利用廉价关系证据，减少得到可靠 Judge 风险估计所需的人工锚点。**

---

## 6. 真正的研究假设

可以把论文核心假设写成三层。

### 假设 H1：关系响应包含错误信息

错误的 Judge verdict 更可能伴随：

- invariance violation；
- degradation blindness；
- locality leakage；
- 较大的反事实响应异常。

即：

\[
P(\text{Judge错误}\mid\text{关系异常})
>
P(\text{Judge错误})
\]

### 假设 H2：关系信息不是重复采样的替代说法

在相同 Judge 调用次数下：

\[
\text{Counterfactual Profile}
>
\text{Repeated Sampling}
\]

如果不控制调用次数，就无法知道提升来自“probe 更聪明”还是“只是调用更多”。

### 假设 H3：关系信息能够减少标签需求

达到相同风险水平时：

```text
普通校准需要 500 条人工标签
关系增强校准只需要 100—200 条
```

这是最有实际价值、也最可能形成顶会贡献的结论。

---

## 7. 为什么这个问题有意义？

## 7.1 科学意义：从平均可靠转向单次可靠

传统研究经常报告：

```text
Judge 与人类总体一致率为 85%
```

但用户真正面对的问题是：

> 当前这一条 8 分，到底是不是那 15% 的错误之一？

平均准确率不能回答单次 verdict 是否可信。

本课题研究的是 per-verdict reliability。

## 7.2 方法意义：测试 Judge 是否“因为正确原因”评分

一个 Judge 可能碰巧与人类一致，但依据的是错误因素。

例如人类因为事实正确给高分，Judge 因为回答更长给高分。

在当前数据上二者一致，但遇到新分布时就会分离。

反事实干预可以帮助区分：

```text
相关性：Judge 与人类碰巧给出同一结果
因果响应：Judge 是否真正响应了应该影响质量的因素
```

## 7.3 工程意义：降低人工复核成本

如果系统能可靠接受 70% 低风险 verdict，只把 30% 高风险样本交给人类：

- 人工工作量明显减少；
- 高风险样本得到更多关注；
- 评测成本可控；
- Leaderboard 更可信。

## 7.4 训练意义：Judge 错误可能进入训练闭环

LLM Judge 不只用于 benchmark，也可能用于：

- 数据筛选；
- rejection sampling；
- RLAIF；
- 自动 reward；
- 模型选择；
- prompt 优化。

如果 Judge 稳定偏爱错误特征，这些偏好会进入训练数据和优化目标，而不是作为随机噪声自动消失。

## 7.5 与 BenchAudit 的关系

BenchAudit 研究：

> benchmark 的题目、答案、rubric、evaluator 是否有问题。

新课题研究：

> 当 evaluator 是 LLM 时，某一次 evaluator verdict 是否值得信任。

共同的核心思想是：

> 不能因为系统给出了高置信度结论，就默认它具有充分证据。

---

## 8. 为什么它可能过于简单或重复？

已有工作已经分别研究：

- 语义等价 paraphrase 下的 Judge 稳定性：[JudgeSense](https://arxiv.org/abs/2604.23478)
- 等价 rubric 改写下的不变性：[Policy Invariance](https://arxiv.org/abs/2605.06161)
- 通过受控退化构造已知偏好方向：[BabelJudge](https://arxiv.org/abs/2606.22329)
- 定向属性退化与多维评分组合：[Attribute-Level Compositionality](https://openreview.net/forum?id=xd9qriSZQh)
- 自动发现 Judge 未知偏差：[BiasScope](https://openreview.net/forum?id=QGOw6AU8Lp)
- 单样本可靠性和 conformal prediction：[Diagnosing LLM Judge Reliability](https://openreview.net/forum?id=FbDuesI6tD)
- accept/escalate 与风险保证：[Trust or Escalate](https://arxiv.org/abs/2407.18370)
- hidden-state linear probe 校准：[Calibrating LLM Judges](https://aclanthology.org/2026.acl-industry.14/)

所以简单版本：

```text
几个 transformation
+ 一个错误分类器
+ accept/defer
```

非常容易被认为只是已有模块的组合。

更有研究价值的升级是：

1. 明确证明纯关系监督不可识别正确性；
2. 显式考虑 transformation 自身可能无效；
3. 研究少量锚点如何与大量关系约束结合；
4. 以标签效率和跨域泛化作为主结果；
5. 在相同 Judge 调用预算下比较。

---

# 9. 从头实施：每一步具体做什么、为什么做

下面给出完整实施流程。每一步都包含：

- 做什么；
- 为什么必须做；
- 输入；
- 输出；
- 如何量化；
- 实施难度；
- 失败意味着什么。

---

## Step 0：先定义“Judge 错误”到底是什么

### 做什么

选择一种清楚的评价任务。第一版推荐使用 pairwise preference：

```text
给定任务 x、回答 A、回答 B 和 rubric，
Judge 判断 A 更好、B 更好或二者相当。
```

定义：

```text
Judge 选择 != 人类多数偏好或客观 verifier
⇒ Judge verdict 错误
```

### 为什么必须做

如果一开始就使用 1—10 分，会遇到：

- 人类 7 分、Judge 8 分是否算错？
- 不同人对分数尺度理解不同；
- 误差阈值容易人为调整；
- 绝对分数校准和内容判断混在一起。

Pairwise 判断的标签更清楚，也更容易从人类偏好数据中获得。

### 输入

- 任务；
- 两个候选回答；
- rubric/reference；
- 人类偏好或客观正确性。

### 输出

统一的数据定义：

```json
{
  "item_id": "x001",
  "prompt": "...",
  "answer_a": "...",
  "answer_b": "...",
  "rubric": "...",
  "gold_preference": "A"
}
```

### 如何量化

先计算原始 Judge：

- pairwise accuracy；
- tie rate；
- position consistency；
- 与人类的一致率。

### 实施难度

低。

### 失败意味着什么

如果无法清楚定义“Judge 错误”，后面所有可靠性预测都没有明确目标，课题不能继续。

---

## Step 1：选择有真实锚点的数据

### 做什么

选择 2—3 类任务，每类都有可用真值：

| 任务族 | 锚点来源 | 优点 | 风险 |
|---|---|---|---|
| 摘要评价 | 人类多维评分 | Judge 场景真实 | 主观分歧较大 |
| 通用指令回答 | 人类 pairwise preference | 接近主流 Judge 用法 | 人类偏好也非绝对真理 |
| 数学推理 | 最终答案 verifier | 真值客观 | 任务相对简单 |
| 代码生成 | 单元测试 | 真值客观 | 执行成本较高 |
| 指令遵循 | 确定性约束 | 容易验证漏项 | 只覆盖部分质量维度 |

### 为什么必须做

关系违反不是错误真值。我们必须知道：

```text
哪些原始 Judge verdict 真的与人类或 verifier 不一致
```

否则只能发表“Judge 行为分析”，无法训练和验证可靠性预测器。

### 输入

- 已有 benchmark；
- 人类标签或执行结果；
- 模型回答。

### 输出

统一的原始样本表。

### 如何量化

记录：

- 总样本数；
- Judge 错误率；
- 人类一致性；
- 不同任务错误率；
- 标签不确定性。

### 实施难度

中等。

主要难点不是下载数据，而是统一不同数据集的标签含义。

### 失败意味着什么

如果 Judge 几乎从不犯错，错误预测没有足够正样本；如果人类标注分歧过大，则无法区分 Judge 错误与任务本身含糊。

---

## Step 2：运行原始 Judge，建立基线

### 做什么

让多个 Judge 对所有原始样本评分。

建议至少覆盖 3—4 个不同模型家族，而不是同一家族的多个尺寸。

保存：

- 原始 verdict；
- score/margin；
- rationale；
- token probability（如果可用）；
- 时间和调用成本；
- Judge 版本和 prompt hash。

### 为什么必须做

这是后续预测的目标对象：

> 我们要判断的就是这一条原始 verdict 是否错误。

同时，不同 Judge 的错误模式可能完全不同。只在一个 Judge 上有效不能证明通用性。

### 输入

- 原始样本；
- Judge prompt；
- Judge 模型。

### 输出

```json
{
  "item_id": "x001",
  "judge_id": "judge_A",
  "verdict": "A",
  "margin": 0.22,
  "rationale": "...",
  "correct": false
}
```

其中 `correct` 只用于评估，不能参与后续 probe 选择。

### 如何量化

- 每个 Judge 的准确率；
- 每个任务的错误率；
- Judge-human disagreement；
- 错误是否集中在少数任务。

### 实施难度

低到中等，主要是 API 成本和输出解析。

### 失败意味着什么

如果不同 Judge 错误完全不重合，这不一定是坏事，但需要 judge-family holdout；如果某个 Judge 接近随机，则它不适合作为主要实验对象。

---

## Step 3：构造质量保持 Probe

### 做什么

构造不应改变回答相对质量的版本，例如：

- 更改 bullet/paragraph 格式；
- 交换无序列表；
- JSON key 重排；
- 等价单位表达；
- 变量重命名；
- 对 rubric 做认证的等价改写；
- 对 A/B 两个回答做相同的无关格式处理。

### 为什么必须做

它测试 Judge 是否受到与任务质量无关的因素影响。

如果两个回答的真实质量关系没有变化，但 Judge 反转偏好，则出现关系异常。

### 输入

- 原始样本；
- transformation specification。

### 输出

```json
{
  "source_item_id": "x001",
  "transform_id": "format_bullets_v1",
  "relation": "preference_invariant",
  "transformed_answer_a": "...",
  "transformed_answer_b": "...",
  "validity_source": "deterministic"
}
```

### 如何量化

- preference flip rate；
- score delta；
- 超过重复采样 jitter 的额外变化；
- 各 transformation family 的 violation rate。

### 实施难度

- 确定性格式变换：低；
- 自然语言等价改写：中高。

### 失败意味着什么

如果所谓“等价改写”改变了答案含义，那么 Judge 翻转不能算 Judge 错误，而可能是 transformation 错误。

---

## Step 4：构造定向退化 Probe

### 做什么

只破坏一个明确质量因素，例如：

- 删除任务明确要求的一项；
- 修改一个关键事实；
- 删除必要引用；
- 在数学答案中替换最终数字；
- 在代码中加入被单元测试捕获的 bug；
- 将正确工具调用参数替换为错误值；
- 制造一句与上下文直接矛盾的话。

### 为什么必须做

Invariance 只能检测“对无关因素过度敏感”。

定向退化检测：

> Judge 是否对真正重要的质量变化足够敏感？

一个 Judge 可能非常稳定，但稳定地忽略关键错误。定向退化正是为了发现这种 blindness。

### 输入

- 原始样本；
- 一个可验证的质量维度；
- 退化操作。

### 输出

带预期方向的关系：

```json
{
  "source_item_id": "x001",
  "relation": "A_should_become_worse",
  "target_dimension": "factuality",
  "verification": "unit_test_failure"
}
```

### 如何量化

- degradation detection rate；
- preference direction accuracy；
- score drop；
- dimension-specific sensitivity；
- 假退化对照下的误响应。

### 实施难度

中等。

代码、数学和显式约束任务较容易；开放摘要和通用对话较难。

### 失败意味着什么

如果无法保证只破坏目标维度，就不能解释 locality；如果无法保证质量确实下降，就不能解释 monotonicity。

---

## Step 5：验证 Transformation 是否真的有效

### 做什么

将 transformation 分成三级：

#### A. 确定性验证

例如：

- JSON key 重排；
- 单位等价；
- 代码变量重命名；
- 单元测试捕获 mutation；
- 删除显式要求字段。

#### B. LLM 生成 + 独立模型验证

例如自然语言 paraphrase：

- 模型 A 生成；
- 不同家族模型 B 判断是否保持语义；
- 工具检查长度、实体和数字；
- 与目标 Judge 隔离。

#### C. 人工抽样

固定抽样一部分：

- 两人独立判断；
- 报告一致性；
- 争议样本删除或标记 uncertain。

### 为什么必须做

这是整个课题最容易被审稿人攻击的位置。

观察到 Judge 分数变化时，存在两种解释：

```text
解释 1：Judge 不可靠
解释 2：Transformation 改变了真实质量
```

不验证 transformation，就无法区分二者。

### 输入

- transformation pairs；
- 独立 verifier；
- 人工检查协议。

### 输出

每条关系的 validity confidence：

```json
{
  "transform_id": "p001",
  "validity_probability": 0.96,
  "validator_agreement": 1.0,
  "human_status": "verified"
}
```

### 如何量化

- transformation acceptance rate；
- 双人一致率；
- Cohen’s κ；
- 各 family 的无效率；
- transformation strength 分布。

### 实施难度

高，是第一大实际瓶颈。

### 失败意味着什么

如果有效率低，不能直接扩大数据；应退回确定性 transformation，或者将边作为带噪关系建模，而不是当作绝对真值。

---

## Step 6：再次调用 Judge，收集关系响应

### 做什么

让相同 Judge 对每个 transformation 后的实例重新裁决。

同时运行等成本重复采样：

```text
Counterfactual：4 个不同关系 probe
Repeated baseline：原始输入额外重复 4 次
```

### 为什么必须做

必须证明关系 probe 提供的不是“多调用几次”的普通收益。

### 输入

- 原始 verdict；
- transformation 后的实例；
- 相同 Judge prompt 和版本。

### 输出

```json
{
  "item_id": "x001",
  "judge_id": "judge_A",
  "probe_responses": {
    "format": "A",
    "paraphrase": "B",
    "omission": "A",
    "fact_error": "B"
  }
}
```

### 如何量化

计算：

- invariance residual；
- monotonicity residual；
- locality leakage；
- order flip；
- repeated variance；
- transformation-specific margin。

### 实施难度

工程难度低，API 调用量中等。

### 失败意味着什么

如果 Judge 输出解析不稳定，需要先修 protocol；如果 malformed output 很多，不能把它们静默丢弃，必须单独报告。

---

## Step 7：构造 Counterfactual Response Profile

### 做什么

将一组 Judge 响应压缩为特征：

```text
原始 margin
重复采样方差
等价改写翻转数
定向退化未响应数
错误维度联动程度
不同 transformation 的响应强度
transformation validity confidence
```

### 为什么必须做

单个 probe 只覆盖一种可能问题。

Profile 的意义是：

> 描述 Judge 在多种受控干预下的行为，而不是只看一次分数。

### 输入

- 原始 verdict；
- 全部 probe verdict；
- relation expectation；
- validity confidence。

### 输出

一个按 `item × judge` 排列的特征表。

### 如何量化

先不训练模型，直接检查：

- 每个特征与 Judge error 的相关性；
- 正确/错误 verdict 的特征分布；
- 单特征 AUPRC；
- 不同任务上的方向是否一致。

### 实施难度

低。

### 失败意味着什么

如果所有关系特征与真实错误都无关，核心假设 H1 不成立，应停止，而不是继续堆复杂模型。

---

## Step 8：使用少量锚点训练错误风险估计器

### 做什么

使用部分人类/客观标签训练：

\[
g(\text{profile})\rightarrow
P(\text{original verdict wrong})
\]

标签预算分别设置为：

```text
25 / 50 / 100 / 200 / 全量
```

模型从简单开始：

1. Logistic Regression；
2. Gradient Boosting；
3. 小型 MLP。

### 为什么必须做

关系一致性不能单独识别“稳定犯错”，少量锚点负责把相对行为连接到人类或客观正确性。

同时，标签预算实验能够直接回答实际问题：

> 关系证据是否真的节省人工？

### 输入

- Counterfactual profile；
- 少量 correctness labels。

### 输出

- 每条 verdict 的错误概率；
- calibrated confidence；
- 模型参数。

### 如何量化

- AUPRC；
- AUROC；
- Brier score；
- ECE；
- 不同标签预算下的学习曲线。

### 实施难度

低到中等。

### 失败意味着什么

如果复杂模型有效、Logistic Regression 无效，要警惕过拟合；如果只在随机切分有效而在新 Judge 上失效，说明模型学到了 Judge 身份或任务特征。

---

## Step 9：建立选择性评测策略

### 做什么

按照预测风险决定：

```text
低风险 → Accept
中风险 → Probe more
高风险 → Defer
```

例如：

```text
risk < 0.05       → Accept
0.05 ≤ risk < 0.2 → Probe more
risk ≥ 0.2        → Defer
```

### 为什么必须做

一个错误预测模型只有转化为决策，才能证明实际价值。

用户并不只想知道 AUPRC，而是想知道：

> 在允许 5% 错误风险时，可以自动接受多少样本？

### 输入

- calibrated risk；
- 用户风险阈值；
- probe 成本；
- 人工成本。

### 输出

- accepted set；
- deferred set；
- 平均调用成本；
- 剩余风险。

### 如何量化

- Risk–Coverage Curve；
- AURC；
- 80% coverage 下的错误率；
- 达到 5% 风险时的最大 coverage；
- 平均 probe 数；
- 人工复核比例。

### 实施难度

低。

### 失败意味着什么

如果模型 AUPRC 提升但 risk–coverage 没改善，说明提升没有转化为有用决策。

---

## Step 10：进行跨 Judge、跨任务、跨 Transformation 测试

### 做什么

至少设置三类严格 holdout。

#### A. Unseen Judge

训练时使用 Judge A/B/C，测试时完全留出 Judge D。

#### B. Unseen Task

训练时使用摘要、指令、数学，测试时使用代码。

#### C. Unseen Transformation

训练时没有“删除引用”probe，测试时全部使用这一新 family。

### 为什么必须做

随机切分很容易泄漏：

- 相同原题的多个变体；
- Judge 身份；
- 任务词汇；
- transformation 模板。

如果只在随机切分有效，模型可能只是记住：

```text
某个 Judge 容易犯错
某个任务更难
某个 transformation 通常翻转
```

而不是真正学会可靠性。

### 输入

- 按 family 分组的数据；
- 冻结 split；
- 未见 Judge/任务/变换。

### 输出

三组泛化结果。

### 如何量化

- 各 holdout 的 AUPRC/AURC；
- 最差任务风险；
- 最差 Judge 风险；
- unseen transformation 增益；
- 相对 IID split 的下降。

### 实施难度

中等。

### 失败意味着什么

- Unseen Judge 失败：信号高度 Judge-specific；
- Unseen Task 失败：信号高度任务特定；
- Unseen Transformation 失败：模型只记住已有 probe 类型。

这三种失败都不等于完全无价值，但需要收窄论文主张。

---

## Step 11：做等预算消融与负对照

### 做什么

在相同额外 Judge 调用数下比较：

1. 无额外调用；
2. 重复采样；
3. 随机 transformation；
4. 固定已知偏差 probe；
5. Counterfactual profile；
6. Multi-Judge disagreement；
7. 所有信号联合。

还需要负对照：

- 随机打乱 relation label；
- 使用确认无关的 transformation；
- 去掉 validity confidence；
- 使用同一模型同时生成 transformation 和 Judge。

### 为什么必须做

它排除几种替代解释：

- 提升只是因为调用更多；
- 提升只是因为答案变长；
- 提升只是任务难度；
- 提升来自模型家族泄漏；
- 提升来自 transformation 模板身份。

### 输入

- 相同样本；
- 相同调用预算；
- 冻结协议。

### 输出

完整消融表。

### 如何量化

- 相对 AUPRC；
- 相对 AURC；
- 相对人工标签需求；
- bootstrap confidence interval；
- paired significance。

### 实施难度

中等。

### 失败意味着什么

如果 counterfactual 不优于等成本 repeated sampling，就没有必要承担 transformation 构造成本。

---

## Step 12：报告排行榜和实际评测影响

### 做什么

用经过风险过滤的 Judge verdict 重新计算：

- 模型胜率；
- 排名；
- Elo；
- rubric 得分；
- 排名置信区间。

### 为什么必须做

即使单样本预测指标提升，也需要证明它会改变真实评测决策。

### 输入

- 原始 Judge verdict；
- accepted/deferred verdict；
- 模型级聚合协议。

### 输出

- 原始排名；
- 风险控制后排名；
- 排名不确定区间；
- 被高风险 verdict 驱动的换位。

### 如何量化

- Kendall’s \(\tau\)；
- pairwise ranking accuracy；
- Top-k regret；
- 排名区间宽度；
- defer 后有效样本比例。

### 实施难度

中等。

### 失败意味着什么

如果单样本错误减少但排行榜几乎不变，应诚实说明该方法主要提升审计可信度，而不是显著改变总体排名。

---

# 10. 数据如何组织

建议至少保存五张逻辑表。

## 10.1 原始实例

```text
data/original_items.jsonl
```

包含：

- task；
- answers；
- rubric；
- reference；
- human/objective label。

## 10.2 Transformation

```text
data/transformations.jsonl
```

包含：

- source item；
- transformed content；
- expected relation；
- target dimension；
- generator；
- validity status。

## 10.3 Judge 原始和 Probe 裁决

```text
results/judgments.jsonl
```

包含：

- Judge 版本；
- prompt hash；
- decoding config；
- verdict；
- rationale；
- usage；
- latency。

## 10.4 Profile

```text
features/profiles.parquet
```

每行对应：

```text
一个原始 item × 一个 Judge
```

## 10.5 Split

```text
protocol/splits.json
```

固定：

- train；
- validation；
- test；
- unseen Judge；
- unseen task；
- unseen transformation。

这样可以防止看到结果后重新选择有利切分。

---

# 11. Pilot 的调用规模和成本可行性

## 11.1 一个中等 Pilot

假设：

- 3 个任务；
- 每个任务 200 条，共 600 条；
- 4 个 Judge；
- 每条 1 次原始裁决；
- 每条 4 个 counterfactual probes。

Judge 调用量：

\[
600\times4\times(1+4)=12000
\]

等预算 repeated baseline 额外需要：

\[
600\times4\times4=9600
\]

总计约：

```text
21,600 次 Judge 调用
+ transformation 生成/验证调用
```

这个规模能够实施，但并不适合作为第一天的尝试。

## 11.2 推荐的微型生死实验

先使用：

- 3 个任务；
- 每类 50 条，共 150 条；
- 3 个 Judge；
- 4 个 probes。

Counterfactual 调用：

\[
150\times3\times5=2250
\]

Repeated baseline：

\[
150\times3\times4=1800
\]

总计约 4,050 次 Judge 调用。

先检查信号方向，再扩到 600 条。

## 11.3 成本如何计算

不应只报 API 次数，还应报告：

\[
\text{总成本}
=
\text{输入 token 成本}
+
\text{输出 token 成本}
+
\text{Transformation 生成成本}
+
\text{验证成本}
\]

不同模型价格会变化，因此正式实验应保存 token usage，并根据运行时实际价格计算，而不是提前写死金额。

---

# 12. 实施可行性评分

| 部分 | 可行性 | 说明 |
|---|---:|---|
| 原始 Judge 调用 | 9/10 | API 和本地模型都容易实现 |
| 确定性 invariance | 9/10 | 格式、顺序、编码变换简单 |
| 客观 degradation | 8/10 | 数学、代码、显式约束较容易 |
| 开放语义 transformation | 5/10 | 是否真正等价最难 |
| Profile 特征 | 9/10 | 统计实现简单 |
| Logistic/GBDT | 9/10 | 不需要大规模训练 |
| Selective evaluation | 8/10 | 方法成熟 |
| Unseen Judge 泛化 | 6/10 | 可能存在较大分布偏移 |
| Unseen Task 泛化 | 5/10 | 是重要实验风险 |
| 顶会新颖性 | 基础版 4/10；升级版 7/10 | 取决于标签效率和关系建模结果 |

### 最大瓶颈

不是代码，而是：

1. transformation validity；
2. 原始 verdict 的可信真值；
3. 等预算下是否真的超过重复采样；
4. 跨 Judge/任务是否仍有增益。

---

# 13. 最有可能出现的实验结果

## 情况 A：理想结果

- Counterfactual profile 显著优于 repeated sampling；
- unseen Judge 仍有效；
- unseen transformation 仍有效；
- 达到相同风险需要的人工标签减少 50%；
- 多个任务方向一致。

结论：

> 具备继续冲击 ICLR/ICML 的理由。

## 情况 B：只有同任务有效

- 随机切分很好；
- leave-one-task-out 失效；
- unseen transformation 失效。

结论：

> 方法主要学习了任务或 transformation 模板，不能声称通用可靠性；可以收窄成领域论文。

## 情况 C：不优于重复采样

- Counterfactual AUPRC 与 repeated sampling 相同；
- 成本更高；
- transformation validity 还需要额外人工。

结论：

> 应停止该方向，因为复杂度没有带来实际增益。

## 情况 D：关系异常多，但不预测真实错误

- Judge 经常发生翻转；
- 但翻转样本与 Judge-human disagreement 无关。

结论：

> 可以形成 Judge sensitivity 分析，但不足以支撑 reliability estimator 主张。

## 情况 E：只在客观任务有效

- 数学和代码有效；
- 摘要和开放式指令无效。

结论：

> 可以收窄为 verifier-grounded Judge auditing，而不是通用 LLM Judge reliability。

---

# 14. 两周执行计划

## 第 1—2 天：冻结问题和协议

完成：

- 选择 pairwise 作为主任务；
- 确定 3 个数据来源；
- 确定 3 个 Judge；
- 固定 4 类 probe；
- 写清楚 success gate；
- 冻结 split。

意义：

> 防止看到结果后修改指标和选择有利数据。

## 第 3—4 天：整理原始数据和标签

完成：

- 统一 schema；
- 检查人类标签；
- 删除高度争议样本或保留软标签；
- 跑原始 Judge。

意义：

> 得到真正需要预测的原始 Judge 错误。

## 第 5—7 天：生成和验证 Probe

完成：

- 2 个确定性 invariance；
- 2 个定向 degradation；
- 人工检查小样本；
- 记录 validity。

意义：

> 确保关系失败不是 transformation 自己造成的。

## 第 8—9 天：收集 Judge 响应

完成：

- Counterfactual 调用；
- 等预算 repeated baseline；
- 输出解析和异常记录。

意义：

> 得到可公平比较的 profile。

## 第 10 天：无模型诊断

检查：

- 关系特征和真实错误是否相关；
- 哪些 probe 有信息；
- 是否只反映任务难度；
- 不同 Judge 方向是否一致。

意义：

> 在训练分类器前确认原始信号存在，防止模型拟合噪声。

## 第 11—12 天：简单预测模型

训练：

- Logistic Regression；
- Gradient Boosting；
- 不同标签预算。

意义：

> 检查关系证据是否减少监督需求。

## 第 13 天：严格 Holdout

完成：

- unseen Judge；
- unseen task；
- unseen transformation；
- cluster bootstrap。

意义：

> 判断是否真正泛化。

## 第 14 天：Go / No-Go

只有满足预注册条件才继续。

---

# 15. 推荐的 Go / No-Go 门槛

建议继续条件：

1. 等调用预算下，AUPRC 至少提高约 5 个百分点；
2. 80% coverage 下错误率相对下降至少 20%；
3. 至少 3 个 Judge 中方向一致；
4. unseen transformation 仍有正增益；
5. 达到相同 AURC 所需锚点数量明显减少；
6. 按原始 item 聚类 bootstrap 后，主要效应区间排除零；
7. transformation 人工抽检有效率足够高。

停止条件：

- 不优于 repeated sampling；
- 只在一个 Judge 有效；
- 只在训练 transformation 有效；
- 结果依赖无效 paraphrase；
- 标签效率没有改善；
- 需要的额外调用成本远高于节省的人工成本。

---

# 16. 如果最终成功，论文到底贡献了什么？

成功版本的论文不是：

> 我们发明了几种反事实改写。

而是：

### 贡献 1：指出关系监督的边界

证明稳定满足关系不等于与人类正确性对齐，纯关系监督存在不可识别性。

### 贡献 2：提出带噪关系可靠性模型

将 transformation validity 和 Judge reliability 分开建模，而不是假设所有变换绝对正确。

### 贡献 3：减少人工锚点

证明达到相同风险需要更少人类标签。

### 贡献 4：跨 Judge 和任务验证

证明信号不是某个 Judge、任务或 transformation 模板特有。

### 贡献 5：转化为真实决策

在相同调用预算下提高 accept coverage 或降低 defer 数量。

---

# 17. 三十秒讲给别人听

> 现在很多 benchmark 用 LLM 给模型回答打分，但总体准确率不能告诉我们某一次评分是否可信。我们的思路是对同一回答做一些受控修改：有的保持质量不变，有的只破坏一个明确要求，然后观察 Judge 是否按预期响应。单纯关系一致仍不能保证 Judge 正确，所以我们用少量人类或客观标签作为锚点，研究这些廉价关系证据能否减少 Judge 校准所需的人类标注，并在相同调用预算下更准确地决定哪些评分可以接受、哪些需要升级复核。

---

# 18. 两分钟讲给导师或审稿人听

> LLM-as-a-Judge 通常只在总体层面报告与人类的一致率，但部署时我们需要知道的是单条 verdict 的风险。重复采样只能发现随机不稳定，无法发现一个 Judge 稳定地忽略某个 rubric 要求。我们通过质量保持和定向退化的反事实 probe，获得 Judge 对不同干预的响应 profile。
>
> 不过，我们不假设关系一致就等于判断正确。一个 Judge 可以稳定但系统性地犯错，因此纯关系监督在理论上不能识别人类意义上的正确性。我们的核心问题是：在只有少量人工或客观锚点时，大量廉价但可能带噪的关系约束，能否提高 per-verdict risk estimation 的标签效率。
>
> 实验会在相同 Judge 调用预算下，与重复采样、多 Judge 分歧和已有置信度方法比较，并严格测试 unseen Judge、unseen task 和 unseen transformation。最终指标不是单纯 flip rate，而是 risk–coverage、AURC，以及达到目标风险需要多少人工标签。

---

# 19. 最终判断

## 实施上是否可行？

可行。

- 不需要训练大模型；
- 不需要搭建复杂环境；
- 可以从 4,000 次左右 Judge 调用的微型 pilot 开始；
- 两周内能够判断核心信号是否存在。

## 最大风险是什么？

不是实现，而是：

- transformation 可能无效；
- 关系异常可能不预测真实错误；
- 提升可能只是来自更多调用；
- 信号可能无法跨 Judge 和任务迁移。

## 为什么仍值得做 Pilot？

因为上述风险都可以在两周内通过严格的等预算实验被证伪，不需要先投入数月。

## 什么版本过于简单？

```text
反事实扰动
+ 统计翻转率
+ 一个普通分类器
```

这个版本过于简单且重复度高。

## 什么版本可能有顶会价值？

```text
关系监督不可识别性
+ transformation validity 建模
+ 少量人工锚点
+ 标签效率
+ 等预算比较
+ unseen Judge/task/transformation 泛化
```

最终值得押注的主张是：

> **反事实关系不是正确性真值，但可以作为一种廉价的结构化证据，在少量锚点下提高 LLM Judge 的风险校准效率。**

